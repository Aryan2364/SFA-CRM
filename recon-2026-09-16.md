# Recon — 16 Sep 2026

Read-only. No code changed, no branch made, no installs, no file moves. The active plan
(`plan-2026-09-16-1522.md`, newest by filename timestamp) was **not edited**; where this
report contradicts it, that is stated and the plan is left alone. The kit at
`D:\RGB_Software\rgb-kit-v2` was read only.

**How main was read.** The `spike/tailwind-4` branch is still checked out and carries 48
uncommitted files. `git checkout main` would have dragged every one of them onto main's
working tree, so main was read through git plumbing instead — `git ls-tree main`,
`git grep main`, `git show main:` — with no checkout. Nothing was read from the spike
branch's working tree and the spike was not revived.

---

## A — Filename collisions, case-insensitive

### A1. Repo files on `main` under `src/components` and `src/lib` — 23 files, exact casing

```
src/components/masters/BusinessPartnerForm.tsx
src/components/ui/CalendarPicker.tsx      src/components/ui/CrudPage.tsx
src/components/ui/Header.tsx              src/components/ui/Modal.tsx
src/components/ui/Pagination.tsx          src/components/ui/RemarksPanel.tsx
src/components/ui/SearchableSelect.tsx    src/components/ui/Sidebar.tsx
src/components/ui/StatusBadge.tsx         src/components/ui/Toggle.tsx

src/lib/auth.ts        src/lib/db.ts          src/lib/mailer.ts
src/lib/permissions.ts src/lib/points.ts      src/lib/r2.ts
src/lib/session.ts     src/lib/session-node.ts src/lib/tenant.ts
src/lib/usage-intelligence.ts  src/lib/visibility.ts
src/lib/generated/date-only-fields.ts
```

All 11 component files are **PascalCase**. All 12 lib files are **lowercase**.

### A2. Kit files — 40 files

`components/ui/` (39): `alert-dialog` `avatar` `badge` `banner` `bar-chart` `breadcrumb`
`button` `calendar` `card` `checkbox` `command` `date-picker` `dialog` `dropdown-menu`
`empty-state` `inline-field-error` `input-group` `input` `label` `pagination`
`password-input` `permission-tooltip` `popover` `radio-group` `searchable-select`
`section-tabs` `select` `separator` `sheet` `skeleton` `sonner` `state-matrix-check`
`switch` `table` `tabs` `textarea` `time-picker` `tooltip` `truncate` — all `.tsx`.

`lib/` (1): `utils.ts`.

All 40 are **kebab-case or lowercase**. `components/` has no subdirectory but `ui`.

### A3. Comparison, lowercased — **the collision set is exactly one**

Kit paths were mapped to where a bulk copy would write them
(`components/ui/x` → `src/components/ui/x`, `lib/x` → `src/lib/x`) before comparing.

| Kit file | Would write | Existing file | Verdict |
|---|---|---|---|
| `components/ui/pagination.tsx` | `src/components/ui/pagination.tsx` | `src/components/ui/Pagination.tsx` | **COLLIDES — casing differs; silent destroy** |

**Total: 1.** No other pair matches case-insensitively. Also checked and clean:

- **Internal collisions within the repo list:** none.
- **Internal collisions within the kit list:** none.
- **Near-miss, safe:** kit `searchable-select.tsx` vs repo `SearchableSelect.tsx`. These do
  **not** collide — the hyphen makes them different filenames — so a bulk copy leaves both
  on disk. Not a data-loss risk, but it does mean the repo would then hold two searchable
  select components, and the existing one has 8 importers (below).
- `src/lib/` is entirely safe: the repo has no `utils.ts`, so the kit's `lib/utils.ts`
  lands on empty ground.

This confirms and bounds the finding from `spike-tailwind4-2026-09-16.md`: `pagination` was
not the first of several, it is the only one.

### A4. Importers of the colliding file

`src/components/ui/Pagination.tsx` — **1 direct importer**:

| Importer | Line |
|---|---|
| `src/components/ui/CrudPage.tsx` | 5 — `import Pagination from './Pagination'` (used at line 174) |

There are 5 textual references to `Pagination` on main in total: the 2 in `CrudPage.tsx`,
2 in `Pagination.tsx` itself (the interface and the export), and 1 unrelated JSX comment in
`src/app/superadmin/(dashboard)/companies/[id]/page.tsx:397`.

**The direct count understates the blast radius.** `CrudPage` is imported by **16** pages,
so destroying `Pagination.tsx` breaks all 16:

```
leads, masters/{dealers, departments, designations, districts, expense-categories,
lead-stages, lead-temperatures, lead-types, product-categories, product-subcategories,
products, states, talukas, users, villages}
```

For the near-miss, `SearchableSelect.tsx` has **8** importers: `BusinessPartnerForm.tsx`
and the masters pages `dealers`, `districts`, `product-subcategories`, `products`,
`talukas`, `users`, `villages`.

---

## B — Does §11 name tokens the kit actually declares?

§11 of the active plan proposes **16** token names. Every one of them is **DECLARED
verbatim** in the kit's `globals.css`. **Zero ABSENT, zero NEAR.**

| §11 token | Status | Kit declaration |
|---|---|---|
| `primary` | DECLARED | `--primary: #174443` |
| `primary-pressed` | DECLARED | `--primary-pressed: #0f3231` |
| `primary-subtle` | DECLARED | `--primary-subtle: #e8f2f1` |
| `primary-border` | DECLARED | `--primary-border: #c6dedc` |
| `primary-ring` | DECLARED | `--primary-ring: #2a6f6c` |
| `text-primary` | DECLARED | `--text-primary: #171717` |
| `text-secondary` | DECLARED | `--text-secondary: #525252` |
| `text-muted` | DECLARED | `--text-muted: #737373` |
| `border` | DECLARED | `--border: #d4d4d4` |
| `border-light` | DECLARED | `--border-light: #e5e5e5` |
| `surface` | DECLARED | `--surface: #ffffff` |
| `surface-sunken` | DECLARED | `--surface-sunken: #fafafa` |
| `surface-control` | DECLARED | `--surface-control: #f5f5f5` |
| `success` | DECLARED | `--success: #166534` |
| `warning` | DECLARED | `--warning: #92400e` |
| `danger` | DECLARED | `--danger: #991b1b` |

(§11 also mentions `badge`; that is a component name, not a token, and is excluded.)

**So §11 introduces no divergence from Sadbhavna by naming a token the kit does not have.**
It is, however, **incomplete** — 13 tokens the kit declares in exactly these families are
not named anywhere in §11, and several are load-bearing rather than optional:

| Not named by §11 | Why it will be needed |
|---|---|
| `primary-hover`, `primary-foreground` | 2 of the 7 brand tokens §2.2 requires be edited together |
| `border-strong` | §6.3 icon-button border states |
| `surface-control-hover`, `surface-control-pressed` | §6.3/§6.4 hover and pressed fills |
| `success-bg`, `warning-bg`, `danger-bg` | §2.4 defines each status as a **text + background pair**; §11 maps only the text half |
| `success-border`, `warning-border`, `danger-border` | badge and banner borders |
| `danger-hover`, `danger-pressed` | §6.1 danger button states |

Two further points of fact, neither of which is a §11 error:

1. **`danger`/`success`/`warning` as single tokens cannot express §2.4.** That section
   defines three status colours as text-plus-background. §11's rows collapse 522 occurrences
   onto one token each, so the mapping is under-specified rather than wrong.
2. **`border` is a token name, not a usable class name.** The kit exposes it as
   `--color-border`, whose Tailwind utility is `border-border`; bare `border` remains the
   width utility. Same shape as the `--text-card` / `--color-card` collision `AGENTS.md`
   §4.1 records. §11's column is a token list, so this only matters when it is turned into
   class rewrites.

**Not acted on. §11 is unchanged.**

---

## C — The kit's token set

**Correction to the spike report:** the count is **201**, not 203.
`spike-tailwind4-2026-09-16.md` used a grep that also matched two lines of prose inside
`/* */` comment blocks. Stripping comments first gives 201, and the four blocks reconcile:
45 + 24 + 131 + 1 = 201.

`app/globals.css` is 512 lines and imports three stylesheets — `tailwindcss`,
`tw-animate-css` and `shadcn/tailwind.css`. **The third matters for any port:** the
`no-scrollbar` class `command.tsx` uses is defined in `shadcn/dist/tailwind.css`, not in
the kit's own CSS.

| Block | Lines | Decls | What it is |
|---|---|---|---|
| `:root` #1 | 13–96 | **45** | The source of truth — every real value |
| `:root` #2 | 113–139 | **24** | shadcn / Base UI vocabulary bridge, all `var()` aliases |
| `@theme inline` | 140–321 | **131** | Exposes the above to Tailwind, plus type scale and sizing |
| `@layer components` | 371–375 | **1** | `[data-sonner-toaster] --width` |

There are also **39 `[data-force]` rules** outside any layer, for the kitchen sink's state
matrix.

**PRODUCT** = changes per product under `AGENTS.md` §0.1. **SYSTEM** = carries across.

### Brand — 7 — **PRODUCT** (§0.1 item 1: all seven edited together)

| Token | Kit value | sfacrm value (plan §3) |
|---|---|---|
| `--primary` | `#174443` | `#3D3A6E` |
| `--primary-hover` | `#1e5a58` | `#4E4A87` |
| `--primary-pressed` | `#0f3231` | `#2D2B52` |
| `--primary-ring` | `#2a6f6c` | `#6864A3` |
| `--primary-subtle` | `#e8f2f1` | `#EDECF4` |
| `--primary-border` | `#c6dedc` | `#D2D0E4` |
| `--primary-foreground` | `#ffffff` | `#FFFFFF` |

### Neutrals — 11 — **SYSTEM** (§2.3)

`--text-primary: #171717` · `--text-secondary: #525252` · `--text-muted: #737373` ·
`--border: #d4d4d4` · `--border-light: #e5e5e5` · `--border-strong: #a3a3a3` ·
`--surface: #ffffff` · `--surface-sunken: #fafafa` · `--surface-control: #f5f5f5` ·
`--surface-control-hover: #e5e5e5` · `--surface-control-pressed: #d4d4d4`

§2.3 documents 8 of these. `--border-strong`, `--surface-control-hover` and
`--surface-control-pressed` are declared in the CSS but absent from §2.3's table.

### Status — 11 — **SYSTEM** (§2.4: "never change when the brand colour changes")

`--success: #166534` · `--success-bg: #dcfce7` · `--success-border: #86efac` ·
`--warning: #92400e` · `--warning-bg: #fef3c7` · `--warning-border: #fcd34d` ·
`--danger: #991b1b` · `--danger-bg: #fee2e2` · `--danger-border: #fca5a5` ·
`--danger-hover: color-mix(in srgb, var(--danger) 88%, #ffffff)` ·
`--danger-pressed: color-mix(in srgb, var(--danger) 85%, #000000)`

The CSS comment marks the last two **"Flagged for ratification"** — §2.4 gives danger one
value, but §6.4 requires hover and pressed for every interactive element, so these two are
derived rather than chosen.

### Chart — 6 — §21 positions, stated in full

| § 21 position | Token | Kit value | Name | PRODUCT / SYSTEM |
|---|---|---|---|---|
| 1 | `--chart-1` | `var(--primary)` → `#174443` | Deep teal (brand) | **PRODUCT** — follows the brand |
| 2 | `--chart-2` | `#c2841d` | Ochre | SYSTEM |
| 3 | `--chart-3` | `#3b5a9a` | Indigo | SYSTEM |
| 4 | `--chart-4` | `#a34a5e` | Rose | SYSTEM |
| 5 | `--chart-5` | `#6fa8a4` | Light teal | SYSTEM |
| 6 | `--chart-6` | `#7a7268` | Warm grey | SYSTEM |

`--chart-1` is the only chart token declared as a `var()` rather than a literal, so it
tracks `--primary` automatically. **This confirms open question §4 item 5 in the active
plan as a real conflict:** with `--primary: #3D3A6E`, position 1 becomes deep indigo while
position 3 stays `#3b5a9a` indigo. Positions 5 and 6 (`#6fa8a4` light teal, `#7a7268` warm
grey) are also tuned to a teal brand. Not acted on.

### Scrollbar — 3 — **ambiguous, flagged**

`--scroll-track: #f0f4f3` · `--scroll-thumb: #a8bdba` · `--scroll-thumb-hover: #7d9895`

§10 gives these as fixed values in a spec table, which reads SYSTEM. But all three are
visibly tinted to the kit's teal brand, and §0.1 does not list them among the five things a
new product changes. **Under an indigo brand they would render as teal furniture.** Stated
as a fact; no change proposed.

### Overlay — 1 — **SYSTEM**

`--backdrop: color-mix(in srgb, var(--text-primary) 40%, transparent)` — §24; derived from
the neutral so no new colour enters the palette.

### Radius — 3 — **SYSTEM** (§5.3)

`--radius-control: 8px` · `--radius-card: 12px` · `--radius-tick: 4px`

### Control heights — 3 — **SYSTEM** (§6.2)

`--h-control: 36px` · `--h-control-sm: 32px` · `--h-control-lg: 40px`

### Type scale, by slot — 12 — **SYSTEM** (§3)

`--text-page-title: 30px` /1.25 · `--text-section: 20px` /1.3 ·
`--text-card-heading: 16px` /1.4 · `--text-body: 14px` /1.6 · `--text-label: 13px` /1.5 ·
`--text-meta: 12px` /1.5 — each with a paired `--…--line-height`.

`--text-card-heading` carries that name deliberately: `--text-card` collided with
`--color-card` and rendered card titles white on white (§4.1).

### Tailwind default type scale, overridden — 18 — **SYSTEM**

`--text-xs/sm/base/lg/xl/2xl/3xl` remapped onto the six slot sizes, and
`--text-4xl` … `--text-9xl` set to `initial` — i.e. **deleted**, so an off-scale size
cannot be reached.

### Font weights — 9 — **SYSTEM** (§3 rule 1)

`--font-weight-normal: 400` and `--font-weight-medium: 500` are the only live values.
`thin`, `extralight`, `light`, `semibold`, `bold`, `extrabold`, `black` are all `initial` —
**`font-semibold` and `font-bold` are removed at the source**, not merely discouraged.
(The repo currently uses them 211 times.)

### Fonts — 3 — **SYSTEM**

`--font-sans` / `--font-heading`: `var(--font-geist-sans)` · `--font-mono`:
`var(--font-geist-mono)`

### Radius aliases — 8 — **SYSTEM**

`--radius-xs/sm/md/lg` → `--radius-control`; `--radius-xl/2xl/3xl` → `--radius-card`;
`--radius-4xl` → `--radius-control`. Every Tailwind radius collapses onto the three §5.3
values.

### Spacing and sizing — 22 — **SYSTEM**

| Token | Value | § |
|---|---|---|
| `--spacing-control` / `-sm` / `-lg` | `var(--h-control…)` | 6.2 |
| `--spacing-search` | `280px` | 11.1 |
| `--spacing-field-min` / `-max` | `160px` / `480px` | 17 |
| `--spacing-dialog-sm` / `-md` / `-lg` | `400px` / `560px` / `800px` | 24 |
| `--spacing-sidebar` / `-rail` | `260px` / `64px` | 12.1 |
| `--spacing-topbar` | `52px` | 12.2 |
| `--spacing-content-max` | `1600px` | 12.3 |
| `--spacing-grid-cell` / `-head` | `120px` / `240px` | 31.1, 31.4 |
| `--spacing-menu-max` | `280px` | 16.2 |
| `--spacing-toast` | `360px` | 25 |
| `--spacing-icon` / `-nav` / `-empty` | `16px` / `18px` / `22px` | 23 |
| `--spacing-tooltip-max` | `240px` | 19 |

`--spacing-icon-nav: 18px` is the legal size the spike's `w-4.5`/`h-4.5` finding lands on.

### Bridge and `@theme` aliases — ~74 — **SYSTEM**

24 shadcn/Base UI names (`--background`, `--foreground`, `--card`, `--popover`,
`--secondary`, `--muted`, `--accent`, `--destructive`, `--input`, `--ring`, `--radius`, and
9 `--sidebar-*`), each a `var()` onto a token above, plus ~50 `--color-*` entries in
`@theme inline` that expose every colour to Tailwind's utility namespace. **No new values;
translation only.**

### Summary

| Kind | Count | Classification |
|---|---|---|
| Brand | 7 | **PRODUCT** |
| Chart position 1 | 1 | **PRODUCT** (via `var(--primary)`) |
| Scrollbar | 3 | **ambiguous — brand-tinted, not listed in §0.1** |
| Neutrals | 11 | SYSTEM |
| Status | 11 | SYSTEM |
| Chart 2–6 | 5 | SYSTEM |
| Overlay | 1 | SYSTEM |
| Radius + aliases | 11 | SYSTEM |
| Control heights | 3 | SYSTEM |
| Type scale + overrides | 30 | SYSTEM |
| Font weights | 9 | SYSTEM |
| Fonts | 3 | SYSTEM |
| Spacing / sizing | 22 | SYSTEM |
| Bridge + `@theme` aliases | 74 | SYSTEM |
| sonner width | 1 | SYSTEM |
| **Total** | **201** | **8 PRODUCT · 3 ambiguous · 190 SYSTEM** |

---

## D — Is the kit's §4.1 pending table stale?

**The table itself is not stale — it is correct and current. Two cross-references to it
elsewhere in `AGENTS.md` are stale.**

### The table

`AGENTS.md` §4.1's "Pending — specified in this file, not yet in this kit" no longer has
eight rows. It has none. It reads:

> **Nothing.** Every rule in this file now names a component this kit ships. … That was not
> true until 16 Sep 2026: eight rules named something that did not exist.

### The eight, and whether each now exists

Named in `SYNC.md` (lines 471–490), one commit each in the kit's git log. **All eight
present and verified in the files, not just claimed:**

| # | The pending item | Now exists? | Verified by |
|---|---|---|---|
| 1 | `in-field` button variant + forced-state rules | **YES** | `button.tsx` lines 8, 14, 63 (`"in-field": [`) |
| 2 | `password-input.tsx` + `eye`/`eye-off` in §23.1 | **YES** | file present; §23.1 map line 1249 |
| 3 | `dialog.tsx` assembling its own body + `alert-dialog` height cap | **YES** | `dialog.tsx` 66–73; `alert-dialog.tsx:96` `max-h-[80vh]` |
| 4 | `permission-tooltip.tsx` | **YES** | file present in `components/ui` |
| 5 | `sticky` opt-in on `TableFooter` + 2 grid spacing tokens | **YES** | `table.tsx` 57–71; `--spacing-grid-cell` 235, `-head` 238 |
| 6 | `recharts` + `bar-chart.tsx` | **YES** | file present; `recharts ^3.10.1` in package.json |
| 7 | `state-matrix-check.tsx` | **YES** | file present |
| 8 | `section-tabs.tsx` | **YES** | file present |

Corresponding kit commits: `26abd20`, `7b74cb7`, `751ce84`, `94dabe2`, `adc92c5`,
`84d6100`, `236e342`, `e9485cb`.

### Does SYNC.md still list them as outstanding?

**No. SYNC.md agrees with the table.** Line 10: *"The section 4.1 component backlog is
CLOSED as of 16 Sep 2026. All eight items landed."* Line 471: *"BOTH halves of the section
4.1 backlog are now synced."*

What SYNC.md **does** still list as outstanding is explicitly not component work, matching
§4.1's closing paragraph: `lib/format.ts` and the §18 formatters, the ES2020 tsconfig
target, `components/templates` (7 files), `components/shell` (6 files), the `globals.css`
autofill rule, the `AGENTS.md` stub trap, the kitchen-sink caption that goes stale when the
formatter lands, and a re-diff of `date-picker.tsx` / `skeleton.tsx`.

### The staleness that does exist — two sentences

Both point at a table that now says "Nothing", and both assert a file or variant that
demonstrably exists:

| Location | Stale text | Reality |
|---|---|---|
| `AGENTS.md` §6.3.1, lines 499–500 | "It is one of the pending items listed in section 4.1: the rule is agreed here, **the variant is not yet in this kit's `button.tsx`**." | `button.tsx` carries `in-field` (item 1 above). The same file's §4.1 row already says button.tsx includes it — **§4.1 and §6.3.1 contradict each other.** |
| `AGENTS.md` §32, line 1687 | "…and anything added later. **It is one of the pending items in section 4.1.**" | `password-input.tsx` exists (item 2 above). |

Not edited. The kit was not modified.

---

## Contradictions with the active plan — recorded, not acted on

1. **§14 of the plan lists `permission-tooltip`, `bar-chart`, `section-tabs`,
   `password-input` and `state-matrix-check` among "missing entirely" components to build.**
   That framing is about *this repo*, which is accurate — but all five exist in the kit as
   shipped files (Section D), so they are copy candidates rather than build work. The
   spike already established 31/31 compile.
2. **§4 item 5 of the plan** (chart position 3 colliding with an indigo brand) is confirmed
   by the actual declarations: `--chart-1` is `var(--primary)` and `--chart-3` is `#3b5a9a`.
   Section C adds that positions 5 and 6 are teal-tuned as well.
3. **§11 is incomplete rather than divergent** — 16/16 declared, 13 sibling tokens unnamed,
   and the three status colours mapped as single tokens where §2.4 defines pairs (Section B).
4. **The "203 tokens" figure** in `spike-tailwind4-2026-09-16.md` and quoted in the plan's
   §14 discussion is **201** (Section C).
5. **The scrollbar tokens** are brand-tinted but not listed by §0.1 as per-product — an
   ambiguity in the kit that neither document currently records.

The active plan has not been modified.

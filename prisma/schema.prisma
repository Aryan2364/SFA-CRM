generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model contextual_remarks {
  id                       String               @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                String               @db.Uuid
  context_type             String
  context_id               String               @db.Uuid
  parent_remark_id         String?              @db.Uuid
  author_user_id           String               @db.Uuid
  body                     String
  created_at               DateTime             @default(now()) @db.Timestamptz(6)
  updated_at               DateTime             @default(now()) @db.Timestamptz(6)
  users                    users                @relation(fields: [author_user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  contextual_remarks       contextual_remarks?  @relation("contextual_remarksTocontextual_remarks", fields: [parent_remark_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  other_contextual_remarks contextual_remarks[] @relation("contextual_remarksTocontextual_remarks")
  notifications            notifications[]
  remark_reads             remark_reads[]

  @@index([tenant_id, context_type, context_id], map: "remarks_context")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model daily_visits {
  id                  String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id           String    @db.Uuid
  user_id             String    @db.Uuid
  visit_date          DateTime  @db.Date
  visit_type          String
  entity_id           String?   @db.Uuid
  entity_name         String
  is_new_entity       Boolean   @default(false)
  start_time          DateTime? @db.Timestamptz(6)
  end_time            DateTime? @db.Timestamptz(6)
  duration_secs       Int?
  latitude            Decimal?  @db.Decimal(10, 7)
  longitude           Decimal?  @db.Decimal(10, 7)
  address             String?
  status              String    @default("Pending")
  notes               String?
  created_at          DateTime  @default(now()) @db.Timestamptz(6)
  updated_at          DateTime  @default(now()) @db.Timestamptz(6)
  end_latitude        Decimal?  @db.Decimal(10, 7)
  end_longitude       Decimal?  @db.Decimal(10, 7)
  end_address         String?
  is_manual_entry     Boolean   @default(false)
  location_flagged    Boolean   @default(false)
  weekly_plan_item_id String?   @db.Uuid
  users               users     @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  orders              orders?
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model departments {
  id           String         @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id    String         @db.Uuid
  name         String
  is_active    Boolean        @default(true)
  created_at   DateTime       @default(now()) @db.Timestamptz(6)
  updated_at   DateTime       @default(now()) @db.Timestamptz(6)
  designations designations[]
  users        users[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model designations {
  id            String      @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id     String      @db.Uuid
  department_id String      @db.Uuid
  name          String
  is_active     Boolean     @default(true)
  created_at    DateTime    @default(now()) @db.Timestamptz(6)
  updated_at    DateTime    @default(now()) @db.Timestamptz(6)
  departments   departments @relation(fields: [department_id], references: [id], onUpdate: NoAction)
  users         users[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model districts {
  id                String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id         String              @db.Uuid
  state_id          String              @db.Uuid
  name              String
  is_active         Boolean             @default(true)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  updated_at        DateTime            @default(now()) @db.Timestamptz(6)
  companies         companies[]
  company_addresses company_addresses[]
  states            states              @relation(fields: [state_id], references: [id], onUpdate: NoAction)
  talukas           talukas[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model expenses {
  id           String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id    String   @db.Uuid
  user_id      String   @db.Uuid
  expense_date DateTime @db.Date
  category     String
  amount       Decimal  @db.Decimal(12, 2)
  notes        String?
  created_at   DateTime @default(now()) @db.Timestamptz(6)
  updated_at   DateTime @default(now()) @db.Timestamptz(6)
  photo_url    String?
  users        users    @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

model lead_temperatures {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id  String   @db.Uuid
  name       String
  sort_order Int      @default(0)
  is_active  Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model notifications {
  id                                      String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                               String              @db.Uuid
  recipient_id                            String              @db.Uuid
  actor_id                                String?             @db.Uuid
  section                                 String
  context_type                            String
  context_id                              String              @db.Uuid
  remark_id                               String?             @db.Uuid
  redirect_path                           String
  message                                 String
  is_read                                 Boolean             @default(false)
  created_at                              DateTime            @default(now()) @db.Timestamptz(6)
  users_notifications_actor_idTousers     users?              @relation("notifications_actor_idTousers", fields: [actor_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users_notifications_recipient_idTousers users               @relation("notifications_recipient_idTousers", fields: [recipient_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  contextual_remarks                      contextual_remarks? @relation(fields: [remark_id], references: [id], onUpdate: NoAction)

  @@index([tenant_id, recipient_id, is_read], map: "notifications_recipient")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model order_items {
  id              String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id       String    @db.Uuid
  order_id        String    @db.Uuid
  product_id      String?   @db.Uuid
  product_name    String
  qty             Int       @default(1)
  rate            Decimal   @default(0) @db.Decimal(12, 2)
  amount          Decimal   @default(0) @db.Decimal(12, 2)
  created_at      DateTime  @default(now()) @db.Timestamptz(6)
  discount_type   String    @default("none")
  discount_value  Decimal   @default(0) @db.Decimal(12, 2)
  discount_amount Decimal   @default(0) @db.Decimal(12, 2)
  gross_amount    Decimal   @default(0) @db.Decimal(12, 2)
  orders          orders    @relation(fields: [order_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  products        products? @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model orders {
  id                    String        @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id             String        @db.Uuid
  user_id               String        @db.Uuid
  visit_id              String?       @unique @db.Uuid
  order_date            DateTime      @db.Date
  total_amount          Decimal       @default(0) @db.Decimal(12, 2)
  created_at            DateTime      @default(now()) @db.Timestamptz(6)
  updated_at            DateTime      @default(now()) @db.Timestamptz(6)
  order_source          String        @default("meeting")
  entity_type           String?
  entity_id             String?       @db.Uuid
  entity_name           String?
  status                String        @default("Confirmed")
  gross_amount          Decimal       @default(0) @db.Decimal(12, 2)
  item_discount_total   Decimal       @default(0) @db.Decimal(12, 2)
  order_discount_type   String        @default("none")
  order_discount_value  Decimal       @default(0) @db.Decimal(12, 2)
  order_discount_amount Decimal       @default(0) @db.Decimal(12, 2)
  has_discount          Boolean       @default(false)
  blocked_reason        String?
  order_items           order_items[]
  users                 users         @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  daily_visits          daily_visits? @relation(fields: [visit_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model product_categories {
  id                    String                  @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id             String                  @db.Uuid
  name                  String
  is_active             Boolean                 @default(true)
  created_at            DateTime                @default(now()) @db.Timestamptz(6)
  updated_at            DateTime                @default(now()) @db.Timestamptz(6)
  deals                 deals[]
  product_subcategories product_subcategories[]
  products              products[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model product_subcategories {
  id                 String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id          String             @db.Uuid
  category_id        String             @db.Uuid
  name               String
  is_active          Boolean            @default(true)
  created_at         DateTime           @default(now()) @db.Timestamptz(6)
  updated_at         DateTime           @default(now()) @db.Timestamptz(6)
  product_categories product_categories @relation(fields: [category_id], references: [id], onUpdate: NoAction)
  products           products[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model products {
  id                    String                @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id             String                @db.Uuid
  name                  String
  category_id           String                @db.Uuid
  subcategory_id        String                @db.Uuid
  price                 Decimal               @db.Decimal(12, 2)
  sku                   String?
  is_active             Boolean               @default(true)
  created_at            DateTime              @default(now()) @db.Timestamptz(6)
  updated_at            DateTime              @default(now()) @db.Timestamptz(6)
  deals                 deals[]
  order_items           order_items[]
  product_categories    product_categories    @relation(fields: [category_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  product_subcategories product_subcategories @relation(fields: [subcategory_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model remark_reads {
  id                 String             @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id          String             @db.Uuid
  remark_id          String             @db.Uuid
  user_id            String             @db.Uuid
  read_at            DateTime           @default(now()) @db.Timestamptz(6)
  contextual_remarks contextual_remarks @relation(fields: [remark_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users              users              @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([remark_id, user_id])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model role_permissions {
  id         String  @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id  String  @db.Uuid
  profile    String
  section    String
  can_view   Boolean @default(false)
  can_edit   Boolean @default(false)
  can_delete Boolean @default(false)
  can_create Boolean @default(false)
  data_scope String  @default("team")

  @@unique([tenant_id, profile, section])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model roles {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id  String   @db.Uuid
  name       String
  is_system  Boolean  @default(false)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  users      users[]

  @@unique([tenant_id, name])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model states {
  id                String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id         String              @db.Uuid
  name              String
  is_active         Boolean             @default(true)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  updated_at        DateTime            @default(now()) @db.Timestamptz(6)
  companies         companies[]
  company_addresses company_addresses[]
  districts         districts[]
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model talukas {
  id                String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id         String              @db.Uuid
  district_id       String              @db.Uuid
  name              String
  is_active         Boolean             @default(true)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  updated_at        DateTime            @default(now()) @db.Timestamptz(6)
  companies         companies[]
  company_addresses company_addresses[]
  districts         districts           @relation(fields: [district_id], references: [id], onUpdate: NoAction)
  villages          villages[]
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model tenants {
  id               String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name             String
  email            String?
  phone            String?
  address          String?
  gstin            String?
  license_count    Int       @default(10)
  payment_status   String    @default("Active")
  payment_due_date DateTime? @db.Date
  is_active        Boolean   @default(true)
  created_at       DateTime  @default(now()) @db.Timestamptz(6)
  updated_at       DateTime  @default(now()) @db.Timestamptz(6)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_territory_mappings {
  id           String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id    String   @db.Uuid
  user_id      String   @db.Uuid
  state_ids    String[] @default([]) @db.Uuid
  district_ids String[] @default([]) @db.Uuid
  taluka_ids   String[] @default([]) @db.Uuid
  village_ids  String[] @default([]) @db.Uuid
  created_at   DateTime @default(now()) @db.Timestamptz(6)
  updated_at   DateTime @default(now()) @db.Timestamptz(6)
  users        users    @relation(fields: [user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([tenant_id, user_id])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model user_visibility {
  id                                          String   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                                   String   @db.Uuid
  viewer_user_id                              String   @db.Uuid
  target_user_id                              String   @db.Uuid
  created_at                                  DateTime @default(now()) @db.Timestamptz(6)
  is_manual                                   Boolean  @default(false)
  users_user_visibility_target_user_idTousers users    @relation("user_visibility_target_user_idTousers", fields: [target_user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
  users_user_visibility_viewer_user_idTousers users    @relation("user_visibility_viewer_user_idTousers", fields: [viewer_user_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([viewer_user_id, target_user_id])
  @@index([tenant_id, viewer_user_id], map: "uv_viewer")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model users {
  id                                                    String                    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                                             String                    @db.Uuid
  name                                                  String
  email                                                 String
  contact                                               String
  department_id                                         String?                   @db.Uuid
  designation_id                                        String?                   @db.Uuid
  profile                                               String                    @default("Standard")
  manager_user_id                                       String?                   @db.Uuid
  status                                                String                    @default("Active")
  created_at                                            DateTime                  @default(now()) @db.Timestamptz(6)
  updated_at                                            DateTime                  @default(now()) @db.Timestamptz(6)
  password                                              String                    @default("")
  password_reset_token                                  String?
  password_reset_expires                                DateTime?                 @db.Timestamptz(6)
  role_id                                               String?                   @db.Uuid
  credentials_version                                   Int                       @default(1)
  attendance                                            attendance[]
  companies_companies_created_by_user_idTousers         companies[]               @relation("companies_created_by_user_idTousers")
  companies_companies_owner_user_idTousers              companies[]               @relation("companies_owner_user_idTousers")
  contacts                                              contacts[]
  contextual_remarks                                    contextual_remarks[]
  daily_visits                                          daily_visits[]
  deals                                                 deals[]
  expenses                                              expenses[]
  notifications_notifications_actor_idTousers           notifications[]           @relation("notifications_actor_idTousers")
  notifications_notifications_recipient_idTousers       notifications[]           @relation("notifications_recipient_idTousers")
  orders                                                orders[]
  remark_reads                                          remark_reads[]
  user_territory_mappings                               user_territory_mappings[]
  user_visibility_user_visibility_target_user_idTousers user_visibility[]         @relation("user_visibility_target_user_idTousers")
  user_visibility_user_visibility_viewer_user_idTousers user_visibility[]         @relation("user_visibility_viewer_user_idTousers")
  departments                                           departments?              @relation(fields: [department_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  designations                                          designations?             @relation(fields: [designation_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users                                                 users?                    @relation("usersTousers", fields: [manager_user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  other_users                                           users[]                   @relation("usersTousers")
  roles                                                 roles?                    @relation(fields: [role_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  weekly_plan_audit_logs                                weekly_plan_audit_logs[]
  weekly_plans_weekly_plans_current_manager_idTousers   weekly_plans[]            @relation("weekly_plans_current_manager_idTousers")
  weekly_plans_weekly_plans_user_idTousers              weekly_plans[]            @relation("weekly_plans_user_idTousers")

  @@unique([tenant_id, contact], map: "users_tenant_contact")
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
/// This model contains an expression index which requires additional setup for migrations. Visit https://pris.ly/d/expression-indexes for more info.
model villages {
  id                String              @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id         String              @db.Uuid
  taluka_id         String              @db.Uuid
  name              String
  is_active         Boolean             @default(true)
  created_at        DateTime            @default(now()) @db.Timestamptz(6)
  updated_at        DateTime            @default(now()) @db.Timestamptz(6)
  companies         companies[]
  company_addresses company_addresses[]
  talukas           talukas             @relation(fields: [taluka_id], references: [id], onUpdate: NoAction)
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model weekly_plan_audit_logs {
  id              String       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id       String       @db.Uuid
  weekly_plan_id  String       @db.Uuid
  actor_user_id   String?      @db.Uuid
  actor_role      String
  action_type     String
  timestamp       DateTime     @default(now()) @db.Timestamptz(6)
  previous_status String?
  new_status      String?
  comment         String?
  edited_fields   Json?
  ip_address      String?
  user_agent      String?
  users           users?       @relation(fields: [actor_user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  weekly_plans    weekly_plans @relation(fields: [weekly_plan_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model weekly_plan_items {
  id                    String       @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id             String       @db.Uuid
  weekly_plan_id        String       @db.Uuid
  plan_date             DateTime     @db.Date
  from_place            String?
  to_place              String?
  new_dealers_goal      Int?         @default(0)
  existing_dealers_goal Int?         @default(0)
  mode_of_travel        String?
  notes                 String?
  created_at            DateTime     @default(now()) @db.Timestamptz(6)
  updated_at            DateTime     @default(now()) @db.Timestamptz(6)
  party_id              String?      @db.Uuid
  party_type            String?
  expected_order_value  Decimal?     @db.Decimal(12, 2)
  weekly_plans          weekly_plans @relation(fields: [weekly_plan_id], references: [id], onDelete: Cascade, onUpdate: NoAction)
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model weekly_plans {
  id                                           String                   @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id                                    String                   @db.Uuid
  user_id                                      String                   @db.Uuid
  week_start_date                              DateTime                 @db.Date
  week_end_date                                DateTime                 @db.Date
  status                                       String                   @default("Draft")
  submitted_at                                 DateTime?                @db.Timestamptz(6)
  last_status_changed_at                       DateTime?                @default(now()) @db.Timestamptz(6)
  current_manager_id                           String?                  @db.Uuid
  manager_comment                              String?
  created_at                                   DateTime                 @default(now()) @db.Timestamptz(6)
  updated_at                                   DateTime                 @default(now()) @db.Timestamptz(6)
  reopen_requested                             Boolean                  @default(false)
  reopen_request_message                       String?
  day_notes                                    Json?                    @default("{}")
  week_goal                                    String?
  weekly_plan_audit_logs                       weekly_plan_audit_logs[]
  weekly_plan_items                            weekly_plan_items[]
  users_weekly_plans_current_manager_idTousers users?                   @relation("weekly_plans_current_manager_idTousers", fields: [current_manager_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users_weekly_plans_user_idTousers            users                    @relation("weekly_plans_user_idTousers", fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([tenant_id, user_id, week_start_date])
}

/// This model contains row level security and requires additional setup for migrations. Visit https://pris.ly/d/row-level-security for more info.
model attendance {
  id                  String    @id @default(dbgenerated("uuid_generate_v4()")) @db.Uuid
  tenant_id           String    @db.Uuid
  user_id             String    @db.Uuid
  date                DateTime  @db.Date
  check_in_time       DateTime? @db.Timestamptz(6)
  check_in_latitude   Float?
  check_in_longitude  Float?
  check_in_address    String?
  check_out_time      DateTime? @db.Timestamptz(6)
  check_out_latitude  Float?
  check_out_longitude Float?
  check_out_address   String?
  created_at          DateTime  @default(now()) @db.Timestamptz(6)
  updated_at          DateTime  @default(now()) @db.Timestamptz(6)
  users               users     @relation(fields: [user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([tenant_id, user_id, date])
}

model expense_categories {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id  String   @db.Uuid
  name       String
  sort_order Int      @default(0)
  is_active  Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)
}

model point_config {
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String    @db.Uuid
  action_type        String
  label              String
  points             Int       @default(0)
  cap_per_day        Int?
  is_active          Boolean   @default(true)
  updated_at         DateTime? @default(now()) @db.Timestamptz(6)
  updated_by_user_id String?   @db.Uuid

  @@unique([tenant_id, action_type])
}

model point_config_history {
  id                 String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id          String    @db.Uuid
  action_type        String
  label              String
  old_points         Int?
  new_points         Int
  old_cap_per_day    Int?
  new_cap_per_day    Int?
  old_is_active      Boolean?
  new_is_active      Boolean
  changed_by_user_id String?   @db.Uuid
  changed_by_name    String?
  changed_at         DateTime? @default(now()) @db.Timestamptz(6)
}

model point_events {
  id          String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id   String    @db.Uuid
  user_id     String    @db.Uuid
  action_type String
  points      Int
  ref_type    String?
  ref_id      String?
  description String?
  earned_at   DateTime? @default(now()) @db.Timestamptz(6)

  @@index([tenant_id, earned_at(sort: Desc)], map: "idx_point_events_tenant")
  @@index([tenant_id, user_id, earned_at(sort: Desc)], map: "idx_point_events_user")
}

model tenant_point_settings {
  tenant_id          String    @id @db.Uuid
  reset_period       String    @default("monthly")
  updated_at         DateTime? @default(now()) @db.Timestamptz(6)
  updated_by_user_id String?   @db.Uuid
}

model company_addresses {
  id                String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String     @db.Uuid
  company_id        String     @db.Uuid
  label             String?
  address_line      String?
  city              String?
  state_id          String?    @db.Uuid
  district_id       String?    @db.Uuid
  taluka_id         String?    @db.Uuid
  village_id        String?    @db.Uuid
  pincode           String?
  latitude          Decimal?   @db.Decimal(10, 7)
  longitude         Decimal?   @db.Decimal(10, 7)
  is_primary        Boolean    @default(false)
  created_at        DateTime   @default(now()) @db.Timestamptz(6)
  updated_at        DateTime   @default(now()) @db.Timestamptz(6)
  business_partners companies  @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  districts         districts? @relation(fields: [district_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  states            states?    @relation(fields: [state_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  talukas           talukas?   @relation(fields: [taluka_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  villages          villages?  @relation(fields: [village_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([company_id])
  @@index([tenant_id])
}

model company_contacts {
  id                String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String    @db.Uuid
  company_id        String    @db.Uuid
  contact_id        String    @db.Uuid
  is_primary        Boolean   @default(false)
  created_at        DateTime  @default(now()) @db.Timestamptz(6)
  business_partners companies @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  contacts          contacts  @relation(fields: [contact_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@unique([company_id, contact_id])
  @@index([contact_id])
  @@index([tenant_id])
}

model contact_types {
  id         String     @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id  String     @db.Uuid
  name       String
  sort_order Int        @default(0)
  is_active  Boolean    @default(true)
  created_at DateTime   @default(now()) @db.Timestamptz(6)
  contacts   contacts[]

  @@index([tenant_id])
}

model contacts {
  id                   String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id            String             @db.Uuid
  name                 String
  mobile               String
  alternate_mobile     String?
  whatsapp             String?
  email                String?
  designation          String?
  contact_type_id      String?            @db.Uuid
  owner_user_id        String?            @db.Uuid
  birthday             DateTime?          @db.Date
  anniversary          DateTime?          @db.Date
  notes                String?
  is_complete          Boolean            @default(false)
  completeness_missing String?
  is_active            Boolean            @default(true)
  created_at           DateTime           @default(now()) @db.Timestamptz(6)
  updated_at           DateTime           @default(now()) @db.Timestamptz(6)
  company_contacts     company_contacts[]
  contact_types        contact_types?     @relation(fields: [contact_type_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users                users?             @relation(fields: [owner_user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  deals                deals[]

  @@index([tenant_id])
}

model custom_field_defs {
  id                  String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String                @db.Uuid
  entity              String
  label               String
  field_type          String
  options             Json?
  sort_order          Int                   @default(0)
  is_active           Boolean               @default(true)
  created_at          DateTime              @default(now()) @db.Timestamptz(6)
  custom_field_values custom_field_values[]

  @@index([tenant_id])
}

model custom_field_values {
  id                String            @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id         String            @db.Uuid
  def_id            String            @db.Uuid
  entity_id         String            @db.Uuid
  value             String?
  created_at        DateTime          @default(now()) @db.Timestamptz(6)
  updated_at        DateTime          @default(now()) @db.Timestamptz(6)
  custom_field_defs custom_field_defs @relation(fields: [def_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([def_id, entity_id])
  @@index([entity_id])
  @@index([tenant_id])
}

model industries {
  id         String      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id  String      @db.Uuid
  name       String
  sort_order Int         @default(0)
  is_active  Boolean     @default(true)
  created_at DateTime    @default(now()) @db.Timestamptz(6)
  companies  companies[]

  @@index([tenant_id])
}

model companies {
  id                                        String              @id(map: "business_partners_pkey") @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id                                 String              @db.Uuid
  type                                      String
  name                                      String
  distributor_id                            String?             @db.Uuid
  mobile_1                                  String?
  address                                   String?
  description                               String?
  state_id                                  String?             @db.Uuid
  district_id                               String?             @db.Uuid
  taluka_id                                 String?             @db.Uuid
  village_id                                String?             @db.Uuid
  latitude                                  Decimal?            @db.Decimal(10, 7)
  longitude                                 Decimal?            @db.Decimal(10, 7)
  is_active                                 Boolean             @default(true)
  created_at                                DateTime            @default(now()) @db.Timestamptz(6)
  updated_at                                DateTime            @default(now()) @db.Timestamptz(6)
  mobile_2                                  String?
  contact_person_name                       String?
  pincode                                   String?
  gst_number                                String?
  sub_type                                  String?
  stage                                     String              @default("Existing")
  temperature                               String?
  next_follow_up_date                       DateTime?           @db.Date
  created_by_user_id                        String?             @db.Uuid
  completeness_missing                      String?
  email                                     String?
  industry_id                               String?             @db.Uuid
  is_complete                               Boolean             @default(false)
  owner_user_id                             String?             @db.Uuid
  website                                   String?
  users_companies_created_by_user_idTousers users?              @relation("companies_created_by_user_idTousers", fields: [created_by_user_id], references: [id], onUpdate: NoAction, map: "business_partners_created_by_user_id_fkey")
  companies                                 companies?          @relation("companiesTocompanies", fields: [distributor_id], references: [id], onUpdate: NoAction, map: "business_partners_distributor_id_fkey")
  other_companies                           companies[]         @relation("companiesTocompanies")
  districts                                 districts?          @relation(fields: [district_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "business_partners_district_id_fkey")
  industries                                industries?         @relation(fields: [industry_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "business_partners_industry_id_fkey")
  users_companies_owner_user_idTousers      users?              @relation("companies_owner_user_idTousers", fields: [owner_user_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "business_partners_owner_user_id_fkey")
  states                                    states?             @relation(fields: [state_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "business_partners_state_id_fkey")
  talukas                                   talukas?            @relation(fields: [taluka_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "business_partners_taluka_id_fkey")
  villages                                  villages?           @relation(fields: [village_id], references: [id], onDelete: NoAction, onUpdate: NoAction, map: "business_partners_village_id_fkey")
  company_addresses                         company_addresses[]
  company_contacts                          company_contacts[]
  deals                                     deals[]
}

model company_types {
  id         String   @id(map: "lead_types_pkey") @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id  String   @db.Uuid
  name       String
  sort_order Int      @default(0)
  is_active  Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)
}

model deal_stages {
  id         String   @id(map: "lead_stages_pkey") @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id  String   @db.Uuid
  name       String
  sort_order Int      @default(0)
  is_fixed   Boolean  @default(false)
  is_active  Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  deals      deals[]
}

model deal_attachments {
  id                  String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String   @db.Uuid
  deal_id             String   @db.Uuid
  file_key            String
  file_name           String
  content_type        String?
  size_bytes          BigInt?
  uploaded_by_user_id String?  @db.Uuid
  created_at          DateTime @default(now()) @db.Timestamptz(6)
  deals               deals    @relation(fields: [deal_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([deal_id])
  @@index([tenant_id])
}

model deal_follow_ups {
  id           String    @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id    String    @db.Uuid
  deal_id      String    @db.Uuid
  due_date     DateTime  @db.Date
  mode         String
  status       String    @default("not_done")
  notes        String?
  completed_at DateTime? @db.Timestamptz(6)
  visit_id     String?   @db.Uuid
  created_at   DateTime  @default(now()) @db.Timestamptz(6)
  updated_at   DateTime  @default(now()) @db.Timestamptz(6)
  deals        deals     @relation(fields: [deal_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([deal_id])
  @@index([tenant_id, due_date])
  @@index([tenant_id])
}

model deal_meetings {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id  String   @db.Uuid
  deal_id    String   @db.Uuid
  visit_id   String   @db.Uuid
  created_at DateTime @default(now()) @db.Timestamptz(6)
  deals      deals    @relation(fields: [deal_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@unique([deal_id, visit_id])
  @@index([tenant_id])
  @@index([visit_id])
}

model deal_stage_logs {
  id                     String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id              String   @db.Uuid
  deal_id                String   @db.Uuid
  from_stage_id          String?  @db.Uuid
  to_stage_id            String   @db.Uuid
  changed_by_user_id     String?  @db.Uuid
  changed_at             DateTime @default(now()) @db.Timestamptz(6)
  days_in_previous_stage Int?
  created_at             DateTime @default(now()) @db.Timestamptz(6)
  deals                  deals    @relation(fields: [deal_id], references: [id], onDelete: Cascade, onUpdate: NoAction)

  @@index([deal_id])
  @@index([tenant_id])
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model deals {
  id                  String              @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id           String              @db.Uuid
  name                String
  owner_user_id       String?             @db.Uuid
  company_id          String?             @db.Uuid
  contact_id          String?             @db.Uuid
  expected_value      Decimal             @default(0) @db.Decimal(12, 2)
  probability         Int                 @default(0)
  deal_stage_id       String?             @db.Uuid
  expected_close_date DateTime?           @db.Date
  product_id          String?             @db.Uuid
  product_category_id String?             @db.Uuid
  source              String?
  remarks             String?
  outcome             String?
  reason_for_loss_id  String?             @db.Uuid
  stage_entered_at    DateTime            @default(now()) @db.Timestamptz(6)
  closed_at           DateTime?           @db.Timestamptz(6)
  is_active           Boolean             @default(true)
  created_at          DateTime            @default(now()) @db.Timestamptz(6)
  updated_at          DateTime            @default(now()) @db.Timestamptz(6)
  deal_attachments    deal_attachments[]
  deal_follow_ups     deal_follow_ups[]
  deal_meetings       deal_meetings[]
  deal_stage_logs     deal_stage_logs[]
  companies           companies?          @relation(fields: [company_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  contacts            contacts?           @relation(fields: [contact_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  deal_stages         deal_stages?        @relation(fields: [deal_stage_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  users               users?              @relation(fields: [owner_user_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  product_categories  product_categories? @relation(fields: [product_category_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  products            products?           @relation(fields: [product_id], references: [id], onDelete: NoAction, onUpdate: NoAction)
  reason_for_loss     reason_for_loss?    @relation(fields: [reason_for_loss_id], references: [id], onDelete: NoAction, onUpdate: NoAction)

  @@index([company_id])
  @@index([contact_id])
  @@index([tenant_id, deal_stage_id])
  @@index([tenant_id])
  @@index([tenant_id, owner_user_id])
}

model journal_entries {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id  String   @db.Uuid
  user_id    String   @db.Uuid
  week_start DateTime @db.Date
  kind       String
  body       String?
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)

  @@index([tenant_id])
  @@index([tenant_id, user_id, week_start])
}

model reason_for_loss {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id  String   @db.Uuid
  name       String
  sort_order Int      @default(0)
  is_active  Boolean  @default(true)
  created_at DateTime @default(now()) @db.Timestamptz(6)
  deals      deals[]

  @@index([tenant_id])
}

model saved_reports {
  id         String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id  String   @db.Uuid
  user_id    String   @db.Uuid
  name       String
  config     Json
  created_at DateTime @default(now()) @db.Timestamptz(6)
  updated_at DateTime @default(now()) @db.Timestamptz(6)

  @@index([tenant_id])
  @@index([tenant_id, user_id])
}

model tenant_settings {
  tenant_id                 String   @id @db.Uuid
  auto_checkout_minutes     Int      @default(0)
  location_flag_threshold_m Int      @default(500)
  deal_stage_ageing_days    Int      @default(15)
  created_at                DateTime @default(now()) @db.Timestamptz(6)
  updated_at                DateTime @default(now()) @db.Timestamptz(6)
  updated_by_user_id        String?  @db.Uuid
}

model weekly_goals {
  id             String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id      String   @db.Uuid
  weekly_plan_id String   @db.Uuid
  text           String
  is_done        Boolean  @default(false)
  sort_order     Int      @default(0)
  created_at     DateTime @default(now()) @db.Timestamptz(6)
  updated_at     DateTime @default(now()) @db.Timestamptz(6)

  @@index([tenant_id])
  @@index([weekly_plan_id])
}

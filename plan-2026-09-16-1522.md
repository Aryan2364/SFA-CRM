# RGB SFA — plan

Supersedes plan-2026-09-16-1515.md on 16 Sep 2026. That plan scoped a
two-day colour-and-primitives subset. Scope is now full compliance with
`AGENTS.md`; see §2 for why.

## 1. HOW TO USE THIS FILE

This section does not change between products. Leave it as written.

**Naming.** Every plan file is `plan-YYYY-MM-DD-HHMM.md` — four digit
year, two digit month, day, then a 24-hour time with **no colon**.
`plan-2026-08-31-1125.md`. Windows forbids a colon in a filename, and a
plan written on a Mac with `11:25` in its name cannot be checked out on
the machine this is built on. The date is when the plan was written, not
when the work happens.

**Supersession.** A plan is never edited into a new plan and never
deleted. When the plan changes materially, write a new file with a new
timestamp and leave the old one in place. The old file is the record of
what was believed at the time, which is the only thing that explains why
a decision was made. At the top of the superseded file add one line:

```
Superseded by plan-YYYY-MM-DD-HHMM.md on DD Mon YYYY.
```

**The pointer.** `plan.md` is two lines and nothing else. It is the only
file anyone has to know the name of:

```
Active plan: plan-YYYY-MM-DD-HHMM.md
Read that file before doing anything in this repository.
```

Update those two lines whenever a new plan supersedes the old one. If
`plan.md` points at a file that does not exist, every session starts by
guessing.

**⚠️ THE POINTER DOES NOT EXIST IN THIS REPOSITORY AND CANNOT.** See §4
item 1. This repo already contains `PLAN.md` — the migration record — and
Windows is case-insensitive, so `plan.md` and `PLAN.md` are one file.
Until that is resolved, the active plan is the newest `plan-*.md` by
timestamp. This is a known deviation from the rule above, recorded rather
than papered over.

**Order of reading.** `AGENTS.md` is the rulebook and outranks this file
on anything to do with design or build. This file is product state: what
is being built, what has been decided, and what is still open. Where they
appear to conflict, `AGENTS.md` wins and the conflict is a bug in this
file.

`AGENTS.md` lives at `D:\RGB_Software\rgb-kit-v2\AGENTS.md` — a sibling
repository, not inside this one. There is no copy here and one must not be
made; a second copy drifts.

---

## 2. PRODUCT

RGB SFA is a multi-tenant sales force automation admin panel: field sales
staff file daily activity, expenses, orders and leads against a weekly
plan, and their managers review, approve and score that work. It replaces
spreadsheet-and-WhatsApp reporting. Indian client, so `AGENTS.md` §18
applies as written — DD/MM/YYYY, lakh/crore digit grouping, `₹`.

This is **an existing product in production being brought into line with
the kit**, not a new build from it. That inverts the usual §0.1 order: the
screens exist and the token layer does not.

**This product goes to the same client who uses Sadbhavna, built from the
same kit. The same people use both.** Colour differs deliberately — RGB
SFA is indigo, and that is the only intended difference. Everything else
follows `AGENTS.md`, which means the two products match automatically
without either being compared to the other.

**That raises the standard, and it is the reason the scope changed.**
Anything left non-compliant does not read as a rule violation in
isolation. It reads as an inconsistency between two products one person
uses in the same week — a button that is a different height, a date in a
different format, an empty state that says "No data" in one product and
offers an action in the other. The subset that would have been acceptable
for a product with no sibling is not acceptable here.

Brand colour: deep indigo `#3D3A6E`. See §3.

---

## 3. LOCKED DECISIONS

1. **The seven brand tokens are settled and final.** Deep indigo. A
   dark-case brand: hovers lighter, presses darker, per `AGENTS.md` §2.2.
   This is a product decision for RGB SFA, not an external client brand.

   | Token              | Value     | Used for                                   |
   |--------------------|-----------|--------------------------------------------|
   | primary            | `#3D3A6E` | Resting state of main buttons, active tabs |
   | primary-hover      | `#4E4A87` | Mouse over. Lighter, not darker            |
   | primary-pressed    | `#2D2B52` | The instant of click                       |
   | primary-ring       | `#6864A3` | Keyboard focus outline                     |
   | primary-subtle     | `#EDECF4` | Selected rows, highlighted panels          |
   | primary-border     | `#D2D0E4` | Borders on primary-subtle surfaces         |
   | primary-foreground | `#FFFFFF` | Text and icons on primary                  |

   `primary-foreground` measures about 9.4:1 on `primary`, above the 4.5:1
   floor §2.2 requires. All seven are edited together as one block.

2. **Scope is full compliance with `AGENTS.md`.** Not a colour pass. The
   responsive pass (§9), the empty and failed states (§13) and the
   permissions audit (§26) are **in scope**, not deferred. See §2.

3. **Verification is against `AGENTS.md`, the kit's own checks, and the
   kit's kitchen sink. Never against the Sadbhavna codebase.** The two
   products match because both follow the specification, not because one
   was copied from the other. Comparing code would propagate Sadbhavna's
   own drift into this product and make its bugs this product's bugs.
   `AGENTS.md` is the specification; `D:\RGB_Software\rgb-kit-v2\app\kitchen-sink\page.tsx`
   is the working reference for what a component looks like and how it
   behaves in every state, including the state matrix.

4. **Neutrals and status colours come from the kit unchanged** —
   `AGENTS.md` §2.3 and §2.4. Status colours do not move when the brand
   moves.

5. **The stack stays.** Next 14.2.35 / React 18 / Tailwind 3.4.1. The kit
   is Next 16.3.3 / React 19.2.8 / Tailwind 4 / shadcn. Upgrading a
   production app mid-retrofit is a second project. Two consequences, both
   load-bearing:
   - **The kit's `globals.css` cannot be copied.** `@import "tailwindcss"`,
     `@theme inline` and `@custom-variant` are Tailwind 4 syntax and
     compile to nothing here. The tokens port as `:root` custom properties
     plus `theme.extend.colors` pointing at them. Same tokens, same single
     source of truth, different declaration syntax.
   - **The kit's 39 components cannot be copied either.** They are shadcn
     over Radix on React 19. They port as specifications — read the
     component and the kitchen sink, rebuild the behaviour — not as files.
     See §14.

6. **The API layer is out of this work entirely.** 115 routes, 7,514
   lines, no UI. The retrofit surface is 51 `.tsx` files / ~12,400 lines,
   not the repo's 21,184.

7. **No second charting library.** `AGENTS.md` §21 names recharts, already
   a dependency here at 3.7.0.

---

## 4. OPEN QUESTIONS

1. **`plan.md` cannot exist in this repository.** Windows is
   case-insensitive and `git config core.ignorecase` is `true`, so
   `plan.md` and the existing `PLAN.md` are the same file. Writing the
   pointer overwrites the migration record. `PLAN.md` is referenced **93
   times across 61 tracked files** — `CLAUDE.md` calls it the source of
   truth for the data layer, and `next.config.mjs`, `prisma.config.ts`,
   nine `scripts/` files and ~45 API routes cite it by section number.
   Renaming it is not a small change. Three ways out, none chosen:
   name the pointer something else and deviate from the template; rename
   the migration record and fix 93 references; or fold the pointer into
   `PLAN.md`'s first lines. **Blocks nothing technical, but every session
   starts by guessing until it is settled.**

2. ~~**The colour mapping in §11 is UNAPPROVED.**~~ **CLOSED 17 Sep 2026 —
   §11 is APPROVED** for the mechanical palette-to-token rewrite, 2,349 of
   2,430 occurrences. The 81 it does not map are in §11.6, each with the
   reason and the screen phase that settles it; none of them is a colour
   question. One is a **kit** question rather than a product one: §2.3 has
   no dark surface token, and 18 occurrences need one (§11.6 B).

3. **The token work instruction never arrived.** The session that
   commissioned the first plan ended with "then do the token work below"
   and nothing below it. The phase order in §6 is derived from the audit,
   `AGENTS.md` and the kitchen sink — not from that instruction.

4. **Seven hue families have no semantic home** — purple (45), violet (4),
   indigo (4), cyan (2), rose (1), pink (1), plus `bg-gray-900`/`800` as
   dark surfaces (17). Named in §11 as UNMAPPED.

5. **Chart position 3 collides with the new brand.** §21's sequence is
   `#174443` deep teal (brand), `#C2841D` ochre, `#3B5A9A` indigo, …
   Position 1 becomes `#3D3A6E` deep indigo, close to position 3's
   `#3B5A9A`. Two of six series stop being distinguishable side by side,
   which is the one thing §21 exists to prevent. Needs a replacement for
   position 3 decided against the real brand. **Sadbhavna will have the
   same six-colour sequence with a different position 1, so this decision
   should hold for the kit, not just for this product** — it likely
   belongs in `AGENTS.md` §21, not here.

6. **The kit does not ship three things `AGENTS.md` specifies**, per §4.1
   and `SYNC.md`: `lib/format.ts` and the §18 formatters, the seven
   `components/templates` page shapes, and the six-file `components/shell`.
   Full compliance needs them. Built here, or built in the kit and
   inherited? Building them here guarantees they differ from Sadbhavna's,
   which is exactly the failure §2 describes.

7. **KIT FINDING — `AGENTS.md` §2.3 and §2.4 are narrower than the kit's own
   `globals.css`.** Found 17 Sep 2026 while re-verifying §11's citations before
   phase 1. **Nothing in this product is wrong and nothing here needs changing;
   this is to be taken to the kit.** Do not edit the kit from this repository.

   Six tokens are declared and working in `rgb-kit-v2/app/globals.css` but do not
   appear in the tables that are supposed to define them:

   | Declared in the kit's `globals.css` | Missing from |
   |---|---|
   | `--border-strong: #a3a3a3` | §2.3's neutral table |
   | `--surface-control-hover: #e5e5e5` | §2.3 — §6.4 does reference it |
   | `--surface-control-pressed: #d4d4d4` | §2.3 — §6.4 does reference it |
   | `--success-border: #86efac` | §2.4's status table |
   | `--warning-border: #fcd34d` | §2.4's status table |
   | `--danger-border: #fca5a5` | §2.4's status table |

   §2.3 tables 8 neutrals where `globals.css` declares 11; §2.4 defines status as
   a text + background pair where `globals.css` declares a border half for each.

   **§11 is right and the kit's tables are behind.** §2.2 settles it: "If the
   table and `globals.css` ever disagree, `globals.css` is right and this table
   is the bug." §11.4's four status rows and §11.6 B's contrast argument (white
   at 2.52:1 on `border-strong`) both read `globals.css` rather than the tables,
   which is the correct precedence — but it means a reader who trusts §2.3 and
   §2.4 alone will conclude those tokens do not exist. §2.2's rule is stated
   inside §2.2, about §2.2's own table; it reads as local to the brand block
   rather than as the general precedence it is being relied on as.

   Two things for the kit, then: table the six tokens, and lift the
   "`globals.css` wins" rule out of §2.2 to cover §2 as a whole.

---

## 5. DATA MODEL

Not restated here. `prisma/schema.prisma` is the source of truth, pulled
from the live database; `CLAUDE.md` and `PLAN.md` carry the tenancy,
scoping and serialisation rules. No money is stored in the retrofit
surface.

---

## 6. PHASE PLAN

**Duration is not two days and should not be quoted as such.** The former
plan's two-day estimate covered colour and primitives only. Scope is now
every section of `AGENTS.md`. Phases 0–3 are estimable; 4 onward are not,
until §14's component count is settled.

A phase is done when it can be demonstrated and has passed §9, not when
the code exists.

- **Phase 0 — settle the open questions.** §4 items 1–2 at minimum; items
  5 and 6 before their phases. *Done when §11 says APPROVED.*
- **Phase 1 — the token layer.** `src/app/globals.css` gains the `:root`
  block: 7 brand + 8 neutral + 3 status + chart + the §3 type scale.
  `tailwind.config.ts` gains matching `theme.extend`, `spacing` restricted
  to the §5.1 scale, and `fontWeight`. That file is 13 lines with an empty
  `extend` today. A `cn()` helper lands with it — see §14's note on the
  tailwind-merge trap, which applies to Tailwind 3 too. *Done when
  `bg-primary` resolves in a built page and the brand swap test moves it.*
- **Phase 2 — the component inventory.** The §14 gap: 11 components here
  against the kit's 39. Build against `AGENTS.md` and the kitchen sink,
  in dependency order — `button`, `input`, `label`, `select`, `table`,
  `badge`, `card`, `dialog` first, since the ten §12 screens need those.
  *Done when each renders every state the kitchen sink's state matrix
  forces.*
- **Phase 3 — the ten files in §12.** 91% of the colour drift. Largest
  first. *Done when §9 checks 1–5a return nothing for the converted file.*
- **Phase 4 — the remaining 42 files.** 212 of 2,438 colour occurrences.
  Mechanical once Phase 3 sets the pattern.
- **Phase 5 — page templates and shell.** §11's seven page shapes, §12's
  sidebar/top bar/content area. Blocked on §4 item 6.
- **Phase 6 — empty, loading and failed states.** §13 (three states, not
  one), §14 (skeletons, never spinners). Previously out of scope.
- **Phase 7 — the permissions audit.** §26. Hide whole areas, disable
  individual actions with a hoverable reason, never show a control that
  fails after being clicked. This product has a real permission system
  (`src/lib/permissions.ts`, the `role_permissions` table), so §26 is
  enforceable rather than cosmetic. Previously out of scope.
- **Phase 8 — the responsive pass.** §9 at 1280, 1024 and 768. The
  sidebar's two behaviours (icon rail below 1280, overlay below 1024), the
  44px tablet touch targets, rule 3's min-width shrinking. Previously out
  of scope.
- **Phase 9 — the long tail of AGENTS.md.** §8 overflow, §15 destructive
  actions, §16 dropdowns, §17 field widths, §18 formats, §19 tooltips,
  §20 pickers, §21 charts, §22 bulk selection, §23 icons, §24 dialogs,
  §25 toasts, §27 search/sort/filter, §29 uploads, §31 data grid,
  §32 passwords, §33 tabs, §34 report tables. Each needs its own audit;
  none has had one.
- **Phase 10 — full §9 pass + brand swap test on every screen.**

---

## 7. LEARNED IN PHASE 1

Empty. Nothing has been built yet.

---

## 8. WHAT A FAST MODEL WILL GET WRONG

1. **Copying the kit's `globals.css` or its components verbatim.** Both
   are Tailwind 4 / React 19 / Radix. See §3 item 5. `globals.css` will
   appear to work — Tailwind 3 ignores `@theme inline` silently — and
   every token will be undefined.
2. **Comparing against the Sadbhavna codebase.** Forbidden, §3 item 3.
   The specification is `AGENTS.md`; the reference is the kitchen sink.
3. **Changing `--primary` alone.** All seven move together, or five shades
   keep the old brand. `AGENTS.md` §2.2.
4. **Mapping `text-gray-400` to `text-muted` as a like-for-like.** It is
   not. See §11 — a contrast fix that visibly changes 200 call sites, and
   that is intended.
5. **Touching `src/app/api/`.** 115 routes, no UI. The one exception is
   the email template in `auth/forgot-password/route.ts`, an HTML string,
   not JSX — and out of scope anyway (§13).
6. **Treating `w-4.5` / `h-4.5` as valid.** 28 occurrences in
   `masters/page.tsx`. Not in Tailwind 3's scale, empty `extend`, so they
   compile to nothing and those icons are unsized today.
7. **Building Add and Edit as separate forms.** `AGENTS.md` §4 rule 1.
   This codebase already gets this right — every master page uses one
   modal with an `editing` state. Do not "improve" it into two files.

---

## 9. SELF-CHECK BEFORE CLOSING ANY SESSION

Commands are the template's, **repathed `app components lib` →
`src/app src/components src/lib`**, which is the stated reason the
template requires: this repo keeps source under `src/`, so the commands as
written match nothing and would report clean against an unconverted
codebase. No check was weakened. Check 4b's known-list is the kit's and
names files that do not exist here; this product's list is stated below it.

Git Bash, from the repository root. The count after each is the **baseline
measured 16 Sep 2026, before any work** — what the check returns today,
not what it should return.

```bash
# 1. No colour outside globals.css. Expect no output.   [baseline: 24 lines]
grep -rnE '#[0-9a-fA-F]{3,8}\b|\brgba?\(|\bhsla?\(' \
  src/app src/components src/lib --include='*.tsx' --include='*.ts'

# 2. No Tailwind palette colours. Expect no output.     [baseline: 1529 lines]
grep -rnE '\b(bg|text|border|ring|fill|stroke|from|via|to)-(slate|gray|grey|zinc|neutral|stone|red|orange|amber|yellow|lime|green|emerald|teal|cyan|sky|blue|indigo|violet|purple|fuchsia|pink|rose)-[0-9]{2,3}\b' \
  src/app src/components src/lib --include='*.tsx'

# 3. No weights above 500. Expect no output.            [baseline: 210 lines]
grep -rnE '\bfont-(semibold|bold|extrabold|black|[6-9]00)\b' \
  src/app src/components src/lib --include='*.tsx'

# 4. No arbitrary bracket values for spacing, radius or type on any
#    screen. Matches ANY utility carrying a bracketed LENGTH; the unit
#    requirement excludes data-[...], transition-[width] and
#    grid-cols-[auto_1fr] without an allow list.        [baseline: 74 lines]
#
#    THIS PRODUCT'S KNOWN LIST: empty. The kit's list names
#    components/ui/input-group.tsx and components/ui/sheet.tsx, neither of
#    which exists here yet. Every current hit is drift to be removed, not
#    a component internal:
#      text-[11px] x29, text-[10px] x22, text-[9px] x1  -> type scale, §3
#      z-[300] x1                                       -> needs a scale
#    When Phase 2 ports input-group and sheet, the kit's two entries
#    become this product's and are added here with that reason.
#    An exception list is for things that cannot be expressed on the
#    scale, not for things nobody got round to fixing.
grep -rnoE '[a-z-]+-\[-?[0-9.]+(px|rem|em|vh|vw|ch|%)?\]' \
  src/app src/components --include='*.tsx'

# 4b. Width and height brackets.                        [baseline: 23 lines]
#     THIS PRODUCT'S EXPECTED LIST: to be written at the end of Phase 4.
#     Currently 23 hits, none yet justified: w-[72px] x6, max-h-[90vh] x5,
#     min-w-[180px] x3, and single uses of w-[480px], min-w-[160px],
#     min-h-[216px], max-w-[180px], max-h-[calc(100vh-280px)],
#     max-h-[85vh], max-h-[80vh], max-h-[70vh], h-[600px].
#     The max-h-[80vh] hits are the §24 dialog cap and will survive. The
#     fixed-pixel widths are §9-rule-2 violations (proportional, never
#     fixed pixels) and will not.
grep -rnE '\b(w|h|min-w|max-w|min-h|max-h|size)-\[' \
  src/app src/components src/lib --include='*.tsx'

# 5. No off-scale spacing on any screen. Expect no output.
#    The scale is 1, 2, 3, 4, 6, 8, 12.                 [baseline: 99 lines]
grep -rnE '\b(p|px|py|pt|pr|pb|pl|m|mx|my|mt|mr|mb|ml|gap|gap-x|gap-y|space-x|space-y)-(5|7|9|10|11|13|14)\b' \
  src/app src/components --include='*.tsx' | grep -v '^src/components/ui/'

# 5a. ADDED for this product. Half-step spacing.        [baseline: 390 hits]
#     Reason: this codebase predates the kit and reaches for 2px/6px/10px/
#     14px steps that a from-scratch kit build never produces, so the
#     template's check 5 was never written to look for them. They are the
#     product's actual spacing problem — py-2.5, py-1.5, py-0.5, mt-0.5,
#     gap-1.5 and friends across 36 files. Expect no output.
grep -rnoE '\b(p|px|py|pt|pr|pb|pl|m|mx|my|mt|mr|mb|ml|gap|gap-x|gap-y|space-x|space-y|w|h|top|right|bottom|left)-[0-9]+\.5\b' \
  src/app src/components --include='*.tsx'

# 6. No dark: utilities. Expect no output.              [baseline: 0 — PASSES]
grep -rn 'dark:' src/app src/components src/lib --include='*.tsx' --include='*.ts'

# 7. The build must pass with no errors.
npm run build
```

**The brand swap test.** `AGENTS.md` §2.2 and the template's §9 carry the
procedure; it is not restated here. Adaptations for this product:

- The magenta edit targets `src/app/globals.css`, not `app/globals.css`.
- `ORIGINAL_PRIMARY` is `'#3D3A6E'`.
- **This product has no `/kitchen-sink` route of its own.** The kit's
  kitchen sink is the reference for what a component should look like; it
  cannot be the target of this product's swap sweep, because it renders
  the kit's components and not this product's. Until a kitchen-sink route
  is built here, run the console sweep on the ten §12 screens plus
  `/login`, and record that coverage is by-screen rather than
  by-component — a component appearing on no screen goes unswept.

The restore step is not optional.

---

## 10. SESSION LOG

```
16 Sep 2026 — Read-only audit of the existing codebase (stack, colour,
components, scale, drift). Wrote plan-2026-09-16-1515.md, then superseded
it with this file when scope widened from a two-day subset to full
AGENTS.md compliance. Verified: every count in §12 and §14 and every
baseline in §9 was measured against the working tree, not estimated. The
kit was read, not copied. Left: no code changed. Phase 0 blocked on §4
items 1-2. NOTE: while writing the pointer file, plan.md overwrote PLAN.md
on the case-insensitive filesystem; restored immediately from git with no
loss, and the collision is recorded as §4 item 1.

17 Sep 2026 — Foundation work on branch foundation/tailwind4-tokens, seven
commits. Built and tested: the pagination case collision resolved by a
two-step rename to the kit's casing before any copy; Tailwind 4 with
tailwind.config.ts deleted rather than left unread; 188 of the kit's 201
tokens ported, brand indigo; all 39 kit components and lib/utils.ts copied
byte-identical; the three screen fixes (28 w-4.5 icons to size-icon-nav, 12
bare borders coloured, 211 font-semibold/bold classified to 400/500); Geist
wired; /kitchen-sink ported. Both kit checks PASS — the §2.2 brand swap swept
1,312 elements with zero stale-brand hits, and §6.4's state-matrix-check was
proved by breaking button:secondary and seeing it named, then restored.
THE ACCEPTANCE GATE FAILED: the kit's components compile and mount on React
18 but do not work. Button carries no forwardRef, so every Base UI trigger
built on it gets a null ref (tooltip, popover/date-picker, dropdown-menu,
sheet), and dropdown-menu hard-crashes the tree via a render-phase setState
in Base UI's MenuGroupLabel. Select works, because its trigger is not Button.
This contradicts spike-tailwind4-2026-09-16.md, which proved compilation and
read Base UI's React 18 peer range as sufficient. Left: two token groups
still blocked on kit rules (chart 2-6, scrollbar), and the React 18 defect is
kit work, not product work — no copied component was edited.

17 Sep 2026 (later) — Completed the token layer once the kit landed §21.1 and
the §10 derivation: chart position 3 substituted to #2481e8 (brand collided at
ΔE00 13.13 against a threshold of 24.28; a green was rejected for sitting 7
from --success), scrollbar derived to #f1f0f4 / #a9a8bd / #7d7c98, 202 tokens.
Took the kit's React 18 fix — forwardRef on button.tsx, Base UI pinned at
1.8.0, kitchen sink re-copied — and proved every affected control opens
positioned on this repo's real React 18.3.1. Merged foundation/tailwind4-tokens
to main, fast-forward. Audited §11 against the finished token layer and found
four failures, the worst being three token names that rendered the wrong colour
silently; corrected all four. §11 APPROVED 17 Sep 2026 for the mechanical
rewrite only: 2,349 of 2,430 occurrences mapped, 81 UNMAPPED and recorded in
§11.6 with the screen phase that settles each. Left: the migration itself has
NOT started — every one of the 2,430 palette classes is still on the screens.
```

---

## 11. COLOUR MAPPING — **APPROVED 17 Sep 2026**

**SCOPE. This section governs the mechanical palette-to-token rewrite and
nothing else.** It says which token replaces which Tailwind palette class. It
does not decide what an element *is* — whether a coloured shape should be a
badge, whether a panel is a surface or a status, whether a colour separates
categories or carries meaning. Those are screen decisions and a colour map
cannot make them: they need the screen in front of you. Everything that turned
out to be one of those questions is listed in §11.6 as UNMAPPED, with the
reason and the file that will settle it.

2,430 palette-class occurrences, 94 distinct colour+shade pairs, 165 distinct
prefixed classes, 15 hue families, collapsing onto the kit's 7 brand + 11
neutral + 11 status tokens. Counts are occurrences, measured on `main` after the
foundation work, excluding the 39 kit components and the kitchen sink.

Corrected 17 Sep 2026 after an audit against the completed 202-token
`globals.css`. Four failures were found and fixed: three token names that
resolved to the wrong colour, 21 classes with no row, the brand and status
addenda that named tokens without mapping anything to them, and two mappings a
rule forbids.

**AMENDED 17 Sep 2026 (later), at the start of the phase-1 screen migration.**
Reading the ten files in context found **104 occurrences with no row that were
also not among the 81 UNMAPPED** — 6% of those files. Two causes, handled
differently:

- **White and black had no rows.** `bg-white` was the only one, and the
  2,430/165 basis excludes white and black entirely. Auditing every call site
  settled 61 of the 104 — see the addendum at the end of §11.1.
- **The remaining 43 are element-identity questions**, the same kind as A–F.
  38 are recorded as **§11.6 G**; the other 5 are foreground halves of elements
  **B** and **D** already defer, and are corrected into those groups.

The approved figure is **not restated**. 2,430 / 2,349 / 81 stands as what was
approved on 17 Sep; the addendum's 247 white/black occurrences are counted
separately in §11.7, and the UNMAPPED headline moves 81 → 86 for the five
corrections. An approved number that changes silently is not an approved number.

**Two rows in this section change a rendered value on purpose.** Neither is a
regression, and both are listed here so a screen review does not read them as
one:

1. `text-gray-400` → `text-text-muted` is a **contrast fix** — §11.1 already
   records it. Those call sites get visibly darker.
2. `bg-black/30` and `bg-black/50` → `bg-(--backdrop)` **normalise their alpha
   to 40%**. One backdrop gets darker, one lighter. §24 is explicit that dialog,
   alert-dialog and sheet all point at one token; 30 and 50 were arbitrary.

### 11.0 THE CLASS NAME IS NOT THE TOKEN NAME — READ THIS FIRST

**Every row below gives the class to write, not the token name.** Where a token's
name already begins with the utility prefix it is written under, the prefix
appears twice, and **that is correct, not a typo. Do not "fix" it.**

| Token | Class to write | A bare `text-primary` instead gives |
|---|---|---|
| `text-primary` | **`text-text-primary`** | `color: var(--primary)` — brand indigo |
| `text-secondary` | **`text-text-secondary`** | `color: var(--secondary)` — `#F5F5F5`, invisible on white |
| `text-muted` | **`text-text-muted`** | `color: var(--muted)` — `#F5F5F5`, invisible on white |
| `border` | **`border-border`** | the 1px width utility, no colour |
| `border-light` | **`border-border-light`** | not emitted |
| `border-strong` | **`border-border-strong`** | not emitted |

The first three are the dangerous ones: `text-primary`, `text-secondary` and
`text-muted` all exist as real classes and render **the wrong colour silently**,
because the shadcn bridge block declares `--secondary` and `--muted` as
`surface-control` aliases. Two of the three paint near-white text on a white
surface. This is the same failure `AGENTS.md` §4.1 records for
`--text-card` / `--color-card`, which rendered card titles white on white: both
namespaces surface through one utility, so they have to stay distinct at the
source and be written out in full at the call site. **862 of the occurrences
below land on those three rows.**

Verified by building a probe page containing every candidate class and reading
the emitted stylesheet. Swept the whole token set for the same shape: only the
six above are affected. `surface*`, `primary*`, `success*`, `warning*`,
`danger*` and `chart-*` do not start with the prefix they are written under, so
`bg-surface`, `bg-primary-subtle`, `ring-primary-ring` and
`outline-primary-ring` are already correct. `bg-success-bg` and
`border-success-border` read oddly but are right — the doubling there is a
suffix, and both resolve.

### 11.1 Neutrals → §2.3

| Current | Class to write | Count | Note |
|---|---|---|---|
| `text-gray-900` `text-gray-800` | `text-text-primary` | 143 | clean |
| `text-gray-700` `text-gray-600` | `text-text-secondary` | 314 | clean |
| `text-gray-500` | `text-text-muted` | 193 | **29 exceptions — see 11.2** |
| `text-gray-400` `text-gray-300` | `text-text-muted` | 212 | **13 exceptions — see 11.2**, and a contrast fix |
| `border-gray-300` | `border-border` | 114 | clean |
| `border-gray-200/100/50` | `border-border-light` | 221 | clean |
| `divide-gray-50` `divide-gray-100` | `divide-border-light` | 6 | same shades as the border row |
| `border-gray-900` | `border-primary` | 1 | active tab accent, §33.3 |
| `bg-white` | `bg-surface` | — | of the 257 white/black utilities |
| `bg-gray-50` | `bg-surface-sunken` | 95 | clean |
| `bg-gray-100` `bg-gray-200` `bg-gray-300` | `bg-surface-control` | 88 | `-200` added: the shades either side already route here |
| `bg-gray-400` `bg-gray-500` | — | 2 | **UNMAPPED, §11.6 A and B** |
| `placeholder-gray-300` | `placeholder-text-muted` | 1 | same shade as the text row |

**`text-gray-400` is a contrast fix, not a rename.** `gray-400` is `#9ca3af`,
about 2.8:1 on white — below the 4.5:1 floor. §2.3 is explicit: `text-muted` is
never set lighter than `#737373`. Those call sites get visibly darker and that is
the point, not a regression.

**`bg-gray-400` and `bg-gray-500` are NOT mapped.** Routing them to
`border-strong` was rejected: a token used outside its named role is exactly the
drift §28 prevents by naming every token for its job. Both are dark fills and
the neutral scale has no dark surface token. They are §11.6 A and B.

#### Addendum, 17 Sep 2026 — white and black

The rows above gave `bg-white` the only white/black row, and the 2,430 / 165
basis excludes white and black entirely. That left **247 occurrences with no
row**, 67 of them in the ten phase-1 files. Every call site in those ten files
was read; these two rows are what that settled.

| Current | Class to write | Count | Scope — outside it, NOT covered |
|---|---|---|---|
| `text-white` **on a `primary` / `primary-pressed` fill, or on a status fill** | `text-primary-foreground` | 42 | any other ground |
| `bg-black/*` **used as a dialog, alert-dialog or sheet backdrop** | `bg-(--backdrop)` | 19 | any other use of `bg-black` |

**Both rows are scoped by what the class sits on, not by the class.** That is
the whole of why they are safe, and it is not a formality:

- **`text-white`.** §2.2 defines `primary-foreground` as "Text **and icons** on
  primary", which is why the two `fill="currentColor"` SVG icons are in scope.
  §11.4 already names this token in prose — "where the red or green is a filled
  button rather than a badge ground, it takes `bg-danger` / `bg-success` **with
  `text-primary-foreground` on it**" — so the 16 status-fill sites were already
  decided and merely had no row to carry them. The 6 sites on other grounds are
  **not** covered: five are corrected into §11.6 B and D below, one into G.
- **`bg-black`.** §24: "The backdrop behind a dialog, alert dialog or sheet is
  the `--backdrop` token… All three components point at that one token; none of
  them defines its own overlay." `--backdrop` is
  `color-mix(in srgb, var(--text-primary) 40%, transparent)`, declared in this
  product's `globals.css`. **A blanket `bg-black/*` row would have been wrong:**
  `contexts/ToastContext.tsx:39` is `bg-black/5` on an `h-1` div — a toast
  progress-bar track, not a backdrop — and the blanket row would have painted it
  as a 40% dialog scrim. It is not covered, and goes to G in its own phase.

All 19 backdrops shift slightly regardless of alpha, because `--backdrop`
derives from `--text-primary` `#171717` rather than pure black. §24 intends
that: "nothing new enters the palette."

### 11.2 §2.3 exceptions — never `text-muted`

§2.3: `text-muted` is **never** a price, a status or an instruction. A sweep of
all three grey text shades found **42 sites** that are exactly those. They take
`text-text-secondary`, not `text-text-muted`. Listed so the migration does not
re-derive them.

**Prices and quantities — 10**

```
(protected)/masters/users/page.tsx:422          (protected)/orders/page.tsx:429
(protected)/orders/page.tsx:685                 (protected)/page.tsx:58
(protected)/points/page.tsx:84                  (protected)/review/[userId]/page.tsx:214
(protected)/settings/points/page.tsx:119        (protected)/weekly-plan/page.tsx:675
superadmin/(dashboard)/companies/[id]/page.tsx:319
superadmin/(dashboard)/companies/[id]/page.tsx:566
```

**Statuses — 24**

```
(protected)/daily-activity/page.tsx:1158        (protected)/daily-activity/page.tsx:1536
(protected)/masters/territory-mapping/[userId]/page.tsx:360, :409, :454
(protected)/orders/page.tsx:339                 (protected)/orders/page.tsx:526
(protected)/orders/page.tsx:686                 (protected)/page.tsx:85
(protected)/page.tsx:322                        (protected)/page.tsx:463
(protected)/review/page.tsx:127                 (protected)/review/[userId]/page.tsx:346
(protected)/settings/access-control/page.tsx:71 (protected)/settings/access-control/page.tsx:753
(protected)/settings/points/page.tsx:186        (protected)/weekly-plan/page.tsx:673
superadmin/(dashboard)/companies/[id]/page.tsx:367, :384, :576, :585, :658, :669
components/masters/BusinessPartnerForm.tsx:306
```

**Instructions and field labels — 8**

```
(protected)/orders/page.tsx:233, :262, :300, :306, :317, :331
(protected)/settings/points/page.tsx:162, :170
```

By shade: `gray-500` 29, `gray-400` 12, `gray-300` 1. **The `gray-500` row was
previously marked "clean" and was not** — it carries "Total Qty", the "Amount"
column header and the `KPICard` default `subColor`.

### 11.3 Brand → §2.2 — all seven tokens

| Current | Class to write | Count |
|---|---|---|
| `bg-blue-600` `bg-blue-500` `bg-blue-400` | `bg-primary` | 54 |
| `bg-blue-700` `bg-blue-800` `bg-blue-900` `bg-blue-950` | `bg-primary-pressed` | 31 |
| *hover state on any primary fill* | `hover:bg-primary-hover` | — |
| `bg-blue-50` `bg-blue-100` `bg-blue-200` | `bg-primary-subtle` | 72 |
| `border-blue-100/200/300/400/500/600/700` | `border-primary-border` | 41 |
| `ring-blue-500` `ring-blue-200` `ring-gray-900` | `ring-primary-ring` | 138 |
| `text-blue-500/600/700/800` `text-blue-400` | `text-primary` | 110 |
| `text-blue-100` | `text-primary-foreground` | 2 |
| `accent-blue-600` | `accent-primary` | 2 |
| `shadow-blue-200` | *delete the class* | 2 | 

`primary-hover` and `primary-foreground` now have rows. `primary-hover` has no
palette class of its own because this codebase writes hovers as
`hover:bg-blue-700`, which the `bg-primary-pressed` row would otherwise swallow —
a hover is not a pressed state (§6.4), so the hover half is split out here.
`text-blue-100` is the label on a selected, primary-filled day cell, which is
text *on* primary and so takes `primary-foreground`.

`ring-gray-900` (27) is a focus ring in the wrong colour and joins the ring row.

### 11.4 Status → §2.4 — by role, both halves

§2.4 defines each status as a **text + background pair**, and the kit declares a
border half as well. One row per role, because 522 occurrences cannot be
rewritten from three single-token rows.

| Family | Role | Class to write | Count |
|---|---|---|---|
| red | text | `text-danger` | 117 |
| red | background | `bg-danger-bg` | 49 |
| red | border | `border-danger-border` | 22 |
| red | ring | `ring-danger` | 1 |
| green + emerald | text | `text-success` | 62 |
| green + emerald | background | `bg-success-bg` | 66 |
| green + emerald | border | `border-success-border` | 10 |
| green + emerald | ring | `ring-success` | 2 |
| amber + yellow + orange | text | `text-warning` | 68 |
| amber + yellow + orange | background | `bg-warning-bg` | 65 |
| amber + yellow + orange | border | `border-warning-border` | 19 |

481 occurrences. A saturated `bg-*-500/600` fill is **not** `bg-*-bg`: where the
red or green is a filled button rather than a badge ground, it takes
`bg-danger` / `bg-success` with `text-primary-foreground` on it, and the danger
button's states are `danger-hover` and `danger-pressed` (§6.1).

Every call site still has to be read for whether it meant the status at all. The
`text-red-500` block (68) is mostly required-field asterisks and icon strokes —
§11.3 rule 3 does require a red asterisk, so those are correct as `text-danger`.

§2.4 also requires status to render as a **badge, never as coloured text**
(§11.1). Much of the text half above is coloured text today, so this mapping is
not complete on its own — the `badge` component is where those call sites land.

### 11.5 Categorical colour → §21's chart palette

Colour that separates one category from another and means nothing in itself has
a vocabulary already: §21's chart palette. `--chart-1` … `--chart-6` are declared
in this product's `globals.css`, including the azure substitute at position 3.

**Only sets that are fixed in code are mapped here.** A set backed by master
data can grow past §21's cap of six, and §21's answer to a seventh — group the
smallest into "Other" — is a decision about data, not about colour. Those sets
are UNMAPPED (§11.6 C).

**The assignment is by §21 position and is not to be reordered.** Position 1
before position 2, and so on, in the order the categories are declared. A
category that changes colour because somebody re-sorted a map is the failure
this is meant to prevent.

| Use | Categories | Assignment |
|---|---|---|
| `SECTION_COLORS`, `conversations/page.tsx:41-43` | 3, fixed in code | `meeting` → `bg-chart-1`, `expense` → `bg-chart-2`, `weekly_plan_day` → `bg-chart-3` |
| Order source, `orders/page.tsx:518, :711` | 2, fixed in code | `direct` → `bg-chart-1`, `meeting` → `bg-chart-2` |
| Masters section cards, `masters/page.tsx:10-12, 45-47, 71-73, 97-99` | 4, fixed in code | sections 1–4 → `bg-chart-1` … `bg-chart-4`, in declaration order |

30 occurrences. The chip foreground on each takes `text-primary-foreground`;
the chart tokens are fills, and §21 says a caller names its series and never
names a colour, so no chip picks its own text colour.

### 11.6 UNMAPPED — 100 occurrences, each with the reason and where it is settled

**None of these is a colour question.** Each one is an element whose identity has
to be decided with the screen in front of you, which is why it is not in the
mapping above. Grouped by the screen phase that will settle it.

#### A. `daily-activity` — the attendance status dots — 3

`daily-activity/page.tsx:1250`, `bg-gray-400` / `bg-amber-500` / `bg-green-500`.

A 10px coloured dot is not a badge, and §11.1 requires status to be a badge;
§7.2 rule 1 requires an icon as well as a colour, and a bare dot is colour alone.
**The semantics are muddled independently of colour**: green for "not checked in
yet", a thing that has not happened, and amber for checked-in-and-working, where
§2.4 reserves warning for "Pending, Due soon, Needs review". Checked-out means
the day is complete, and §2.4 lists Completed under success.

It becomes a badge and the three states get named, in this file's screen phase.

#### B. The dark surface hole — 22 — **a KIT question, not an sfacrm one**

`settings/access-control/page.tsx:781` (`bg-gray-500`), plus 17 `bg-gray-900` /
`bg-gray-800` elsewhere.

**Corrected 17 Sep 2026: 18 → 22.** Four `text-white` occurrences are the
**foreground halves** of these same dark fills — `companies/[id]:683`, `:722`,
`masters/users:324` (all on `bg-gray-900`) and `access-control:782` (the
standalone org-chart band on the `bg-gray-500` this group already argues about
at :781). They were missed because §11 had no white row at all. A fill and the
text on it are one decision: if the dark fill has no token, neither does its
foreground, and mapping the text to `primary-foreground` while the ground stays
`gray-900` would assert this is a brand surface when the whole point of this
group is that nobody has said what it is. Same reasoning that put the fills
here; same phase settles both halves together.

**These are one hole, not two.** §2.3's neutral scale runs from `surface`
`#FFFFFF` to `surface-control-pressed` `#D4D4D4` — every one a light fill. There
is no dark surface token, and all 18 sites are dark fills carrying light text.
The org-chart band is the clearest case: it is the de-emphasised twin of the
in-chain card's `bg-blue-800` band, it carries `text-white`, and white measures
**4.83:1** on the current `gray-500` against **1.09:1** on `surface-control` and
**2.52:1** on `border-strong`. No existing neutral can carry it.

Raise with the kit: §2.3 needs a dark surface token, or a rule saying dark fills
are not part of this system. Not settled in this product.

#### C. Expense-category chips — 28 — master-data-backed

Two copies of `CATEGORY_COLORS`: `daily-activity/page.tsx` and
`review/[userId]/page.tsx`, 12 palette classes each plus a 2-class fallback.

Six categories are hardcoded — Travel, Food, Accommodation, Phone, Stationary,
Miscellaneous — but `expense_categories` is master data with a CRUD page and API,
so the real set is whatever an administrator has created. A seventh category
falls to the grey fallback silently today. §21 caps at six and answers a seventh
by grouping the smallest into "Other", which is a decision about the data and
cannot be made by a colour map.

Settled in the `daily-activity` and `review` screen phases, together, because
the two maps must not diverge.

#### D. Both avatar implementations — 12 — §4 rule 2 before it is a colour

| | `RemarksPanel.tsx:41` | `settings/access-control/page.tsx:877` |
|---|---|---|
| Palette | 6: blue, emerald, purple, amber, teal, rose | 5: blue, purple, green, orange, pink |
| Key | `userId`, summed char codes | `name.charCodeAt(0)` |
| Survives a rename | yes | **no** |

Different palettes and different keys, so **the same person is a different
colour in the remarks panel than in the org chart**, and the org-chart one also
changes when a user is renamed and collides for every name sharing an initial.

This is §4 rule 2 — an element on more than one screen exists once in code — so
it is a component consolidation before it is a colour choice. One avatar
component, one key, then a palette. The kit ships `avatar.tsx`.

**Corrected 17 Sep 2026: 11 → 12.** `access-control:884` is `text-white` on
`${color}` — the initials drawn on whichever `AVATAR_COLORS` entry the key
picked. It is the **foreground half** of the same element, and it cannot be
decided before the palette it sits on is. Same reasoning that put the fills
here.

#### E. Four purple uses that are not categorical — 13

| Use | Site | What it actually is |
|---|---|---|
| "Edited by Manager" chip | `(protected)/page.tsx:93` | a weekly-plan **status**, one of seven; §2.4 has no token for it |
| Weekly-plan banner | `weekly-plan/page.tsx:453-454` | a §7.1 **banner** — a condition that persists until resolved |
| Territory-mapping selection | `masters/territory-mapping/[userId]/page.tsx:421, :430` | a **selection state**; §16.2 says selected is `primary-subtle` |
| Leads type chip | `leads/page.tsx:29` | **decoration** — every lead type renders the same purple, so it separates nothing |

Routing any of these to a chart token would paint a status, a banner and a
selection state with categorical colour, which is the same error as §21's "a red
bar meaning third quarter". Each settled in its own file's screen phase.

#### F. Stray families — 8

`indigo`, `cyan`, `rose`, `pink` and friends, scattered. Almost certainly
accidents. Each settled where it is found.

#### G. The phase-1 residue — 52 — found by reading the ten files in context

**Added 17 Sep 2026, at the start of the phase-1 screen migration.** §11 was
derived from counts. Phase 1 is the first time each call site is read with the
screen in front of it, and 104 occurrences in the ten files turned out to have
no row and to be in none of A–F. 61 were white and black and are settled by the
§11.1 addendum; 5 were foreground halves corrected into B and D. **G1–G5 are the
remaining 38, and every one is an element-identity question, not a colour one** —
which is why they belong here and not in a row.

**G6 is 14 more, and is a different kind.** G1–G5 have no row. G6 *has* a row
and the row is wrong — it renders the signal invisible. Those 14 come out of the
approved 2,349, which is why this is the one amendment that **reduces** the
approved figure rather than sitting beside it. See §11.7.

**G is the ten files only.** The remaining 42 files have not been read in
context yet and will add to this group as their phases run. One is already
known: `contexts/ToastContext.tsx:39`, `bg-black/5` on an `h-1` toast
progress-bar track — not a backdrop, so outside the §11.1 addendum's scope.

**G1. The teal location panels — 16.** `daily-activity:300, :353, :354, :362,
:365` and `review/[userId]:383, :412, :413, :417, :419`. Teal is not a status
family, is not one of §11.5's three fixed-in-code sets, and is not one of F's
strays. The panels show GPS start/end location, and the teal is doing a job
nobody has named: `:362` and `:417` already switch to `bg-red-50 border-red-200`
on a location mismatch, so teal is carrying "matched" against danger's
"mismatched" — a status pair in everything but vocabulary. If that is what it
means it is `success`, and it should be a badge per §11.1. Settled in the
`daily-activity` and `review` phases, together, because the two panels are the
same element twice (§4 rule 2).

**G2. Purple beyond E's 13 — 13.** E's count is correct and closed; these are
different elements that happen to share the hue.

| Site | What it is | Why no row fits |
|---|---|---|
| `daily-activity:226`, `review:360` | the "New" entity badge | one condition, one colour — decoration, not a category and not a status |
| `review:345` | `typeColor` — Dealer / Distributor / other | **3 categories fixed in code.** This is exactly §11.5's shape and passes its stated test, but §11.5 names three sites and this is not one. Adding it is a §11.5 decision, not a phase-1 one. |
| `review:290` | the "Suggest Changes" **button fill**, `bg-purple-600` / `hover:bg-purple-700`, plus its `text-white` | sits beside `bg-red-600` Reject and `bg-yellow-500` Hold. §11.4 handles saturated red and green fills; **there is no purple status token.** "Suggest" is a seventh weekly-plan state, the same gap E records for "Edited by Manager" — §2.4 has three roles and the workflow has seven. |
| `territory-mapping:423, :440, :450, :453` | `accent-purple-600` ×2, `text-purple-600`, `ring-purple-500` | part of the same selection UI as E's `:421, :430`, but outside the lines E names — see the note below |

**G3. The `accent-` prefix on status families — 3.**
`territory-mapping:378, :405` (`accent-green-600`) and `:462`
(`accent-orange-500`). **These are the clearest case for why G is deferred
rather than mapped.** They are the checkbox accents for the three levels of the
district → taluka → village hierarchy: green, purple, orange. The colour is
**categorical by hierarchy level, not status.** §11.4 maps `text` / `bg` /
`border` / `ring` on green to `success` — had `accent-` been swept in with them,
two of these would have become `accent-success` and painted "this row is a
taluka" in the colour of "Approved". The missing row is protecting the screen.
`accent-` is also not reachable by the token layer the way the four mapped
prefixes are, which is its own question.

**G4. Gradient stops — 4.** `daily-activity:218` and `review:355`,
`from-amber-400 to-orange-400`. §11 has no gradient row anywhere, and the kit
has no gradient vocabulary — §2 is flat fills. Whether this element keeps a
gradient at all is a screen decision.

**G5. Coloured shadows — 2.** `daily-activity:217` (`shadow-amber-50`) and
`access-control:809` (`shadow-yellow-100`). §11.3 already settled the only other
coloured shadow — `shadow-blue-200`, **delete the class** — on the grounds that
the kit has no coloured shadow. The same answer is likely, but `access-control:809`
is paired with `border-yellow-400` as the org-chart **search-match highlight**,
which is a state that has to keep a signal if the shadow goes. Settled with that
screen.

**G6. A row that does not fit — 14 — a different kind from G1–G5.**

**G1 to G5 have no row at all. These fourteen HAVE one, and it is wrong.** The
distinction is the whole reason this sub-group is written out: a later session
re-running the mapping mechanically will reach these occurrences and find
§11.4's background row waiting for them. **This note is what stops it applying
one.**

**The cause.** §11.4's rows carry a **role** — text, background, border — and the
background role was measured against a **badge ground**, where the token sits
behind dark text and *the text carries the signal*. At 1.10:1 on white that is
fine, because nothing is being asked of the background itself. **Where the shape
IS the signal — a dot, a stripe, a bar, a legend swatch — the same token has
nothing left.** Measured on this product's own tokens:

| | on `surface` `#ffffff` | on `surface-control` `#f5f5f5` |
|---|---|---|
| `success-bg` `#dcfce7` | **1.10:1** | **1.01:1** |
| `warning-bg` `#fef3c7` | **1.11:1** | — |
| `emerald-400` `#34d399`, as it renders today | 1.92:1 | 1.76:1 |

This is the same failure §11.0 records for `text-muted` and `text-secondary` — a
token used outside the role it was measured for — arriving through the
background half instead of the text half.

| Sites | What it is | What §11.4's background row would do |
|---|---|---|
| `daily-activity:219`, `review/[userId]:356` | `h-1` card status stripe, the Completed state | a 1px `#dcfce7` line on white. Its **Active twin at `:218` / `:355` is already G4**, so the row would migrate half a stripe |
| `daily-activity:985, :986`; `companies/[id]:269–271`; `companies/[id]:282–284` | 8px legend dots — a 2-series and a 3-series set, both **fixed in code** | success + warning + brand in one legend |
| `daily-activity:1125, :1148` | `w-1.5 h-4` accent bars marking **Plan** against **Actual** | breaks the pair: blue → `bg-primary` (dark indigo), emerald → `bg-success-bg` (near-white). One bar visible, one not |
| `companies/[id]:251` | progress-bar fill on a `bg-gray-100` track | `#dcfce7` on `#f5f5f5` — **1.01:1**. Invisible |
| `daily-activity:1565` | pulsing "1 active" dot | `#fef3c7` on white. Same shape as A's attendance dots — colour alone, no icon, §7.2 rule 1 |

**Three of the five are not status at all, and this is what each needs when its
screen phase settles it** — recorded here rather than re-derived later:

- **The legend dots and the Plan/Actual pair are categorical.** They separate
  series and mean nothing in themselves. That is §11.5 and §21's chart palette.
  Both sets are fixed in code, so §21's cap of six is not at risk and neither is
  blocked the way §11.6 C is — they are simply not among §11.5's three named
  sites, and adding them is a §11.5 decision.
- **The progress bar is magnitude, not category and not status.** §21: "For a
  single measure across a range… use a sequential ramp from light teal to deep
  teal in five steps instead." That is its answer, not a status token.
- Only the card stripe and the activity dot are status at all, and both are
  §11.1's "status is always a badge, never plain coloured text" — the same
  question as A.

**Not in this group: `daily-activity:142` and `review/[userId]:102`.** The
day-cell "has activity" dot is `bg-blue-500`, going `bg-blue-200` when the cell
is selected. `bg-primary` on white and `bg-primary-subtle` on the primary fill
is coherent in both states, so it maps normally.

**A note on E's count, since phase 1 raised it.** E's 13 is **correct and
arithmetically closed**: `(protected)/page.tsx:93` (2), `weekly-plan:453, :454,
:458` (4), `territory-mapping:421, :430` (5), `leads:29` (2). The
`territory-mapping` occurrences at `:423`, `:440`, `:450` and `:453` are
genuinely outside it. E is not wrong — but it **under-describes the element**:
it names the selection's background and link colours and not the checkbox
accents, the count text or the search ring, which are the same selection UI. The
territory-mapping phase settles E and G2's four rows together, or it will fix
half a screen.

#### Not palette classes, recorded elsewhere

12 recharts hexes in `(protected)/page.tsx` — superseded by the §21 chart tokens,
which now exist. 20 hexes in `api/auth/forgot-password/route.ts` — an HTML email,
out of scope per §13.

### 11.7 Count

Measured on `main` at the commit that approved this section.

| | Occurrences |
|---|---|
| **Mapped by §11** | **2,349** |
| **UNMAPPED** (§11.6) | **81** |
| **Total** | **2,430** |

**This table is the figure approved on 17 Sep 2026 and is deliberately not
restated.** The amendment of the same date is counted separately below, so a
later reader sees the amendment as an amendment. An approved number that changes
silently is not an approved number.

#### Amendment count, 17 Sep 2026

**Basis A — as approved.** The 2,430 above. 94 distinct colour+shade pairs, 165
distinct prefixed classes, **white and black excluded** — that exclusion is what
makes 165 resolve, and it is why `bg-white` carries a `—` in its §11.1 row.

**Basis B — the white/black addendum.** 247 occurrences repo-wide across three
classes (`bg-white` 137, `text-white` 86, `bg-black` 24), on the same scope:
`main` after the foundation work, excluding the 39 kit components and the
kitchen sink.

| | Occurrences |
|---|---|
| Basis A — mapped by §11 as approved | 2,349 → **2,335** |
| Basis A — UNMAPPED, §11.6 A–G | 81 → **100** |
| **Basis A total (approved 17 Sep)** | **2,430**, unchanged |
| Basis B — white/black, mapped by the §11.1 addendum | 218 |
| Basis B — white/black, UNMAPPED | 29 |
| **Basis B total (amendment 17 Sep)** | **247** |
| **Combined** | **2,677** |

**This is the one amendment that reduces the approved figure rather than sitting
beside it, and that is deliberate.** Everything else on 17 Sep added a basis or
moved an occurrence between groups; the 2,349 itself stood. §11.6 G6's fourteen
come *out* of it: 2,349 → **2,335 mapped**, UNMAPPED 86 → **100**, G 38 → **52**.
The 2,430 total does not move.

**Why this corrects the count rather than changing scope.** A row that renders a
signal invisible — `success-bg` at 1.10:1 on white under a 1px stripe, 1.01:1
under a progress bar — was never a correct mapping. It was a mapping derived
from a count, applied to a role it had not been measured against. Removing it
does not narrow what §11 covers; it removes fourteen occurrences §11 was never
entitled to claim. The 17 Sep audit could not have caught it, because catching it
needs the call site, and phase 1 is the first time each call site is read.

Basis B's split is measured in the ten phase-1 files and projected for the other
42, which have not been read in context. In the ten files it is exact: 67
white/black occurrences, 61 mapped by the addendum, 6 UNMAPPED (5 into B and D,
1 into G).

**UNMAPPED headline: 81 → 86 → 100.** The first five are the foreground halves
corrected into §11.6 B (18 → 22) and D (11 → 12) — not new deferrals, but the
other half of elements those groups already defer. **§11.6 G is 52**, the ten
files only: 38 with no row (G1–G5) and 14 whose row is wrong (G6).

**Verification of the basis.** The regex was re-run on `main` at the start of
phase 1 and reproduces §11's structural facts exactly: **94** distinct
colour+shade pairs, **165** distinct prefixed classes once white and black are
excluded (168 with them), **39** kit components excluded, and §11.6 **B = 18**
and **F = 8** to the occurrence. Families matched are the 22 Tailwind palette
hues; prefixes are `bg text border ring divide placeholder accent shadow from to
via fill stroke outline decoration caret`.

96.7% mapped. The 2,430 is the same regex the first audit ran; it measured 2,423
before the foundation work and 2,430 now — twelve `border-gray-200` added by the
bare-border fix, less five that left scope when `Pagination.tsx` became the
kit's. The audit's 2,438 headline used a slightly wider scope. The 39 kit
components and the kitchen sink contribute zero palette classes, which is what
being token-only means.

---

## 12. THE TEN FILES HOLDING 91% OF THE DRIFT

**Re-measured 17 Sep 2026 at the start of phase 1.** The previous figures in
this section — 2,226 of 2,438, and the per-file column running 463 / 342 / 240 —
were **carried verbatim from the superseded `plan-2026-09-16-1515.md`** and never
re-measured when §11.7 was. §11.7 already flagged the headline ("the audit's
2,438 headline used a slightly wider scope"); the per-file column had the same
problem and was not flagged. It is corrected here so the next session does not
re-derive it.

**The files did not change** — all ten line counts still match the original
audit exactly. It was purely a counting-method difference.

**Method.** Occurrences of `<prefix>-<family>-<shade>`, plus `<prefix>-white` and
`<prefix>-black`, where family is one of the 22 Tailwind palette hues, shade is
one of `50 100 200 300 400 500 600 700 800 900 950`, and prefix is one of
`bg text border ring divide placeholder accent shadow from to via fill stroke
outline decoration caret`. Scope is tracked `.tsx` / `.ts` under `src/`,
excluding the 39 kit components in `components/ui` and the kitchen sink. This
reproduces §11's structural facts — see the verification note at the end of
§11.7.

Convert in this order, largest first.

| # | File | Colours | Lines |
|---|------|---------|-------|
| 1 | `src/app/(protected)/daily-activity/page.tsx` | 363 | 1,685 |
| 2 | `src/app/superadmin/(dashboard)/companies/[id]/page.tsx` | 266 | 764 |
| 3 | `src/app/(protected)/review/[userId]/page.tsx` | 189 | 656 |
| 4 | `src/app/(protected)/orders/page.tsx` | 188 | 744 |
| 5 | `src/app/(protected)/weekly-plan/page.tsx` | 156 | 740 |
| 6 | `src/app/(protected)/settings/access-control/page.tsx` | 141 | 889 |
| 7 | `src/app/(protected)/page.tsx` | 135 | 514 |
| 8 | `src/app/(protected)/masters/users/page.tsx` | 120 | 556 |
| 9 | `src/app/(protected)/masters/territory-mapping/[userId]/page.tsx` | 89 | 535 |
| 10 | `src/app/(protected)/leads/page.tsx` | 88 | 333 |

**1,735 of 2,692 — 64%, not 91%.** The old 91% was an artefact of the wider
scope. **`review/[userId]` and `orders` swap places**: review is ahead on the
corrected basis, so the conversion order changes at positions 3 and 4.

File 7 also holds all 12 recharts hexes and is blocked on §4 item 5.

The remaining 42 files hold 957 occurrences between them.

---

## 13. OUT OF SCOPE

**The responsive pass, the empty and failed states, and the permissions
audit are NO LONGER out of scope.** They were, in the superseded plan.
They are now Phases 8, 6 and 7. §2 is why.

One item remains out of scope:

1. **The email template** — `src/app/api/auth/forgot-password/route.ts`,
   20 hardcoded hexes. It is an HTML string sent by SMTP, not JSX. Email
   clients do not support CSS custom properties, so it cannot use the
   token layer and needs its own approach. It is also the only UI a
   recipient sees outside the app, so it should not stay indefinitely on
   the old blue — record it as deferred, not dismissed.

---

## 14. COMPONENT INVENTORY GAP

`AGENTS.md` §4.1 lists **39** components in `components/ui`, and states
that table is the whole of it. This product has **11**, none of them
built against the kit.

| This product (`src/components/`) | Nearest kit component |
|----------------------------------|-----------------------|
| `ui/CalendarPicker.tsx`          | `calendar.tsx` + `date-picker.tsx` |
| `ui/CrudPage.tsx`                | no equivalent — a page template, §11.1 |
| `ui/Header.tsx`                  | `components/shell` (not shipped, §4 item 6) |
| `ui/Modal.tsx`                   | `dialog.tsx` |
| `ui/Pagination.tsx`              | `pagination.tsx` |
| `ui/RemarksPanel.tsx`            | no equivalent — product-specific |
| `ui/SearchableSelect.tsx`        | `searchable-select.tsx` |
| `ui/Sidebar.tsx`                 | `components/shell` + `sheet.tsx` |
| `ui/StatusBadge.tsx`             | `badge.tsx` |
| `ui/Toggle.tsx`                  | `switch.tsx` |
| `masters/BusinessPartnerForm.tsx`| product-specific; already one form for Add and Edit, per §4 rule 1 |

**Missing entirely, and each one is a rule that currently has no
implementation here:** `button`, `input`, `label`, `select`, `table`,
`card`, `checkbox`, `radio-group`, `textarea`, `tooltip`,
`permission-tooltip` (§26), `empty-state` (§13), `skeleton` (§14),
`banner` (§7.1 — **this is Alert; there must never be an `alert.tsx`**),
`inline-field-error`, `alert-dialog` (§15), `dropdown-menu`, `popover`,
`breadcrumb`, `avatar`, `separator`, `tabs`, `section-tabs` (§33),
`truncate` (§8), `password-input` (§32), `time-picker` (§20.2),
`input-group`, `command`, `sonner` (§25), `bar-chart` (§21),
`state-matrix-check`.

The absence of `button` and `input` is what produced the 130 raw
`<button>` and 92 raw `<input>` in screens. They are a symptom, not the
disease.

**`lib/utils.ts` and the `cn()` trap, which applies here too.** §4.1
records that `cn()` is not plain tailwind-merge: it is extended with the
type-scale token names. tailwind-merge treats any unrecognised `text-*`
class as a text colour, and the type scale lives in the `text-*` namespace
too — so `cn("text-label", "text-text-secondary")` classified both as
colours, kept the last, and dropped the font size silently. Every label
rendered at the inherited size with no error anywhere. **Anything added to
the type scale is added to the `cn()` extension in the same commit.**
Related: a type-scale token must never share a name with a colour token —
`--text-card` collided with `--color-card` and card titles rendered white
on white, which is why the kit's token is `--text-card-heading`.

This product has no `cn()` at all today, and no `clsx`, `classnames`,
`tailwind-merge` or `cva` in `package.json`. Phase 1 introduces it with
the extension already in place, rather than discovering the trap later.

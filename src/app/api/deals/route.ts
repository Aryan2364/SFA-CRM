/**
 * `GET /api/deals` (list, §4.2/§4.8) and `POST /api/deals` (create, §4.1).
 *
 * The implementation is in `_handlers.ts` — see the note there.
 */
export { GET, POST } from './_handlers'

export const dynamic = 'force-dynamic'

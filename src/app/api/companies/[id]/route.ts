/** `GET`/`PUT`/`DELETE /api/companies/[id]`. See `../route.ts` for why the
 *  implementation sits in `_handlers.ts`. */
export { GET, PUT, DELETE } from './_handlers'

export const dynamic = 'force-dynamic'
